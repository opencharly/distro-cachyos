# 2026.249.1529 — refactor(cachyos): the unified from:name:tag clone + the canonical recording surface (Phase 3)

## Summary

The Phase 3 **unified clone spelling** conversion (Cutover A, batch: Phase 3):

1. `check-vm-clone` + `check-instrument-cachyos-vm` deploy `from: check-vm-clone-base:golden` — the loader splits the tag; the vm-build step drives `charly vm build check-vm-clone-base --from-snapshot golden` (the deploy-hop resolves the base bed s chain to the cachyos-vm entity; the disk materializes as a clone of the base s golden).
2. The `cachyos-vm-clone` entity (`source.kind: clone`) is DELETED (the entity arm retires via plugin-vm #25).
3. `cachyos-vm` (the distro root) gains the ONE canonical recording surface (spice channel + graphics + video - inherited by every clone/deploy, R3).

## How tested (pasted, LIVE)

The converted instrument bed runs green on this tree (target: disposable: true):

```
$ charly check run check-instrument-cachyos-vm
PASS (steps=9) - exit=0, 113s
  vm-build | vm-create | deploy-add | check-live | update | cleanup - ALL green
  diagnostics: errors 0, warnings 0, allowlisted 31, UN-allowlisted 0
  evidence (evidence.yml):
    screen  spice  live  frames.mjpeg (49) -> frames.mp4 h264 1280x800
    term    record live  .cast (asciinema v3, prompt -> exit -> code 0)
$ charly box validate
charly box validate: OK - checked 505 candies, 40 boxes, 36 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

## Rulebook compliance

- R0: git-workflow skill loaded; the spike-and-adopt iteration (strict-policy) followed.
- R1: the registry-keying reality + the deploy-hop gaps RCA d live + fixed in the wave (sdk #218-#220, spec #101/#102, plugin-vm #24-#26, plugin-check #26/#27, deploy-vm #10/#11).
- R2: same named batch (Phase 3).
- R3: one recording surface per distro root; one clone spelling (from: name:tag).
- R4: no workarounds.
- R5: the clone entity is deleted.
- R10: the converted instrument bed ran green (pasted above) with the full instrument matrix + the transcode.

## Change classification

Class: bed conversion (Phase 3). Gate: the instrument bed s live R10 run (pasted) + the full-tree validation. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*