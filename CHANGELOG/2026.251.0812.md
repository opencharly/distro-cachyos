# 2026.251.0812 — fix(pin): cachyos box — layer-android-apidemos v2026.239.1626 → v2026.250.1816 (the committed-ApiDemos-APK fix)

## Summary

Pin bump in the cachyos box (`charly.yml`, `check-android-emulator-pod` → `device-net`):
`@github.com/opencharly/layer-android-apidemos:v2026.239.1626` → **`v2026.250.1816`**.

The 239.1626 pin baked the pre-fix ApiDemos acquisition (apk-pure download);
v2026.250.1816 carries the committed-APK fixture fix (commit `6eb6860` —
`tests/data/ApiDemos-debug.apk` pushed over goadb, no apkeep, no external source),
which is what the E-5 lane's `device-net` member must install on the running emulator.

## How tested

**Tag existence (the pinned ref resolves):**

```
gh api repos/opencharly/layer-android-apidemos/git/refs/tags/v2026.250.1816
=> v2026.250.1816 -> 6eb68607cc26 (the committed-ApiDemos-APK fix commit);
   layer-android-apidemos main contains 6eb6860 (compare 6eb6860...main: ahead, 0 behind)
```

**The layer's fixed check at the pinned tag:**

```
layer-android-apidemos @ v2026.250.1816 charly.yml:
  apk:
    - apk: tests/data/ApiDemos-debug.apk
  # "No source: — committed files don't use apkeep"
  check … the ApiDemos package is installed on the device after the target:android deploy
  check … the installed ApiDemos package resolves a launchable activity
```

**Local gate:** `charly.yml` re-parses clean after the one-line pin edit (yaml.safe_load OK);
the repo's CI (charly/pr-validator) validates the box at the repo's pinned charly.
Reference wave bed run `check-android-emulator-pod/2026.251.0554` (disposable: true): the
E-5 lane's device/device-net install path (goadb committed-APK push + `pm install`)
executed live with the identical committed-file mechanism — the 1816 delta is exactly the
fixture source swap this pin applies.

## Rulebook compliance

- **R1**: the stale pin (239.1626, pre-fix acquisition) is root-caused to the apidemos
  committed-APK fix landing at v2026.250.1816 — this is the consumer pin sync, no guesswork.
- **R3**: no duplication — the layer owns the fixture; the box only pins its tag.
- **R4**: no workarounds — no hand-pin/replace-hash; the released tag is the pin.
- **R6**: git safety — feat/ branch only, no force-push, no direct main push.
- **R10**: pins are merged-ref synced with evidence above; the E-5 wave consumer run is the
  bed witness (named above, `disposable: true`).

## Change classification

- **Change class**: fix (consumer pin sync — android-wave pre-fix tag)
- **Verification gate**: tag-existence + fixed-check grep at the tag + YAML gate (above)
- **Attribution tier**: analysed on a live system (the wave bed ran live; full 4-phase
  E-5 gate incl. the appium instrument finalize rides the consumer E-5 PR)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (95% confidence)*