# 2026.251.0816 — fix(pin): E-5 device — layer-android-test-apps v2026.239.1624 → v2026.251.0811 (the committed-F-Droid-APK fix)

## Summary

Pin bump in the cachyos box (`charly.yml`, `check-android-emulator-pod` → `device`):
`@github.com/opencharly/layer-android-test-apps:v2026.239.1624` → **`v2026.251.0811`**.

The 239.1624 pin baked the pre-fix F-Droid acquisition (the APKPure download path —
blocked by APKPure's Cloudflare bot-wall, HTTP 403). v2026.251.0811 carries the
committed-canonical-client fix (layer-android-test-apps PR #2, merged):
`tests/data/F-Droid.apk` pushed over goadb — no apkpure/apk-pure anywhere.

## How tested

**Tag existence (the pinned ref resolves):**

```
gh api repos/opencharly/layer-android-test-apps/git/refs/tags/v2026.251.0811 => 7b1e822c
Tree at the tag: tests/data/F-Droid.apk  (the committed canonical client from f-droid.org,
sha256 985f5181d48bb6bafd54083a048b391271e0ab28385881cc41294fb01a222762)
```

**The layer's fixed behavior at the tag (its own PR #2 fresh-rebuild bed run, pasted there):**

```
check-android-emulator-pod run 2026.251.0554 (check-live + feature-run + rebuild cycle,
4/4 phases, disposable: true target):
  PASS  check adb=shell  [apk-fdroid-present]  package:org.fdroid.fdroid
  PASS  check adb=shell  [apk-fdroid-launch]   <pid>
```

**Local gate:** `charly.yml` re-parses clean after the one-line pin edit; the repo's CI
(charly/pr-validator) validates the box at the repo's pinned charly.

## Rulebook compliance

- **R1**: the 239.1624 pin is root-caused to the APKPure-blocked F-Droid install (the
  E-5 bring-up kill); the fix landed + released as v2026.251.0811 — this PR is the consumer sync.
- **R3**: no duplication — the layer owns the fixture; the box pins its released tag.
- **R4**: no workarounds — released-tag pin, no hand-pin/raw-sha.
- **R6**: git safety — feat/ branch only, no force-push, no direct main push.
- **R10**: merged-ref pin with the live-bed evidence above (disposable: true; the full
  4-phase E-5 gate incl. the appium instrument finalize rides the consumer E-5 PR).

## Change classification

- **Change class**: fix (consumer pin sync — android-wave pre-fix tag)
- **Verification gate**: tag-existence + fixed-check grep at the tag + YAML gate (above)
- **Attribution tier**: analysed on a live system (the E-5 bed ran live; the full
  4-phase gate incl. the appium instrument finalize rides the consumer E-5 PR)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (95% confidence)*