# 2026.247.1647 — chore: align openclaw/selkies family pins to merged newest tags

## Summary

Aligns the openclaw/selkies family pins to their merged newest tags, closing the multi-repo dual-pin chain (`pod-selkies` resolves at both v2026.243.1737 and v2026.246.2210) that blocked the charly cachyos-import re-vendor. Six coordinated upstream pin alignments landed first (layer-wl-record/screenshot-pixelflux v2026.247.1546, pod-kde-selkies v2026.247.1546, pod-selkies-core v2026.247.1638, pod-selkies-desktop v2026.247.1642, layer-selkies-kde-desktop v2026.247.1643, marketplace corpus); this PR points the distro boxes and closure at those tags.

Changes (all ref-only):
- boxes: pod-selkies-desktop .243.1850/.247.0314 → v2026.247.1642 (openclaw-desktop, selkies-labwc, selkies-labwc-nvidia); layer-selkies-kde-desktop .243.1848 → v2026.247.1643 (selkies-kde, selkies-kde-nvidia); layer-openclaw-full .246.1954 → v2026.247.0718 (openclaw-desktop, openclaw-full).
- closure (candy/charly-marketplace): pod-selkies .1737 → .246.2210; wl-pixelflux .1823/.1830 → v2026.247.1546; pod-kde-selkies .1836 → v2026.247.1546; pod-selkies-core .1844 → v2026.247.1638; layer-selkies-kde-desktop → v2026.247.1643; layer-openclaw-full → v2026.247.0718.

## How tested

```
$ charly box validate   # distro-cachyos, this head
charly box validate: OK — checked 505 candies, 40 boxes, 35 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

The full graph (505 candies across 40 boxes / 35 deploys) resolves at **0 warnings, 0 errors** — the dual-pin warning is gone, not suppressed. The upstream tags are the merge-emitted CalVers listed above (verified via gh api tags). The charly cachyos-import re-vendor (the coordinated downstream gate) re-runs on the new distro tag.

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow skill loaded before the pin change. |
| R1 — RCA every anomaly | The dual-pin warning traced through a six-repo stale-pin cascade (each old tag's content resolved pod-selkies at .1737); every edge aligned at its source repo first. |
| R3 — no duplication | One canonical newest version per candy. |
| R4 — no workarounds | Pin to the merged tags; no suppressions, no overrides. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | box validate 0/0 pasted (real run on the committed tree). |
| R10 — fresh disposable proof | Resolution validated at 0/0 on the committed tree. |

## Change classification

Dependency pin alignment (config-only, box + closure ref sync). **Attribution:** analysed on a live system — the confidence value for this change class (full graph resolved at 0/0 on a live run of the fresh binary; the full-gate task verify / re-vendor is the coordinated downstream follow-on). Footer confidence: high confidence.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (high confidence)*