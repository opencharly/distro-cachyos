# 2026.250.1349 — check-cachyos-mcp-vm: the systemd + full-MCP R10 bed (native pac install via the cachyos-vm mcp_provide)

## Summary

The CachyOS VM check bed (plan §3.9) — the standing R10 gate for the charly package's HOST-install path:

1. **cachyos-vm entity** — declares `mcp_provide` (P4: a kind:vm declares its MCP servers like a box; the mcp: verb resolves it for VM venues via the check env — plugin-mcp #6 + plugin-check #32 + charly #555, merged) + `port_forwards: - "18765:18765"` (the MCP port host-forwarded on the FIXED port — the deterministic target the addr probe dials).
2. **check-cachyos-mcp-vm bed** — deploys the CachyOS VM, installs charly as a native pac package, composes the **charly-mcp-enable candy** (the charly-native deploy-time start: `service: use_packaged: charly-mcp.service, enable: true` → `systemctl enable --now` at deploy — the check phases are verify-only, so the start must be a deploy-time action), and asserts via PROPER verbs: `file:` (units + config, the unit's 127.0.0.1 P3 default via `contains`), `service:` (enabled + running), `addr:` (the forwarded port reachable — a deterministic literal, see below), `mcp:` (ping/list-tools/call), `command:` (charly box validate -C /etc/charly).
3. **charly-mcp-enable candy** — `use_packaged` + `enable: true` + `scope: system`. The SHIPPED unit binds 127.0.0.1 (P3 host default, asserted by the bed's unit-file check). The deploy plan's `write-bind-dropin` root `command:` step lands a systemd drop-in (`install -d` + `printf` → `/etc/systemd/system/charly-mcp.service.d/charly-bind.conf`, `--listen 0.0.0.0:18765`) + `restart-mcp` reloads + restarts — the RUNNING server binds all interfaces so the host-side mcp: checks reach it through the forwarded port; the shipped unit stays untouched. This reconciles the bind addresses: shipped 127.0.0.1, runtime 0.0.0.0 (normal inside a VM; the host is the security boundary). The `addr` probe dials the FIXED forwarded host port as a literal (127.0.0.1:18765 + `eventually: 90s`) — the check-live venue never threads the HOST_PORT env var, so the template form SKIPped the probe; the literal is deterministic.

**Version reconciliation:** the bed's `add_candy` pins layer-charly v2026.243.0408 — that is the LAYER's version; the charly BINARY the VM runs comes from the localpkg which DOWNLOADS the PUBLISHED package from the per-distro repos at the current release (v2026.250.0854 — verified in-VM by `mcp-charly-call-version` → 2026.250.0854). The published arch package ships the units + `/etc/charly/charly.yml` with `systemctl is-enabled charly-mcp` = disabled (the non-autostart contract, asserted by the per-distro install-tests).

## How tested

**R10 — the fresh-rebuild disposable-VM bed run** (`charly check run check-cachyos-mcp-vm` against the VERIFIED STACK: the charly built from origin/main with the plugin-check VM-venue env threading + the sdk deploy-compile drop-in fix, the new plugin-mcp v2026.250.0721, bed tree @ bcc7dd9). Verbatim:

```
EXIT:     0
CALVER:   2026.250.1311 (the log-clean proof run: 0x Permission denied / 0x here-document / 0x CHARLY_ACT_EOF in deploy-add.log + update.log)
STEPS:
  vm-build           ok=true  157s   vm-create  ok=true  1s   deploy-add ok=true  91s
  bring-up-members   ok=true  0s      check-live ok=true  15s   update    ok=true  84s
  check-live-rebuild ok=true  14s     cleanup    ok=true  3s     cleanup-members ok=true 0s
OVERALL:  ok — summary.yml: ok:true, errors: 0, warnings: 0 (allowlisted: 53), total_seconds: 365

Per-check matrix (check-live, identical in check-live-rebuild; VM ssh cachy@127.0.0.1):
  PASS  [systemd-system-unit]        packaged system unit installed + binds loopback only (P3)       ok
  PASS  [systemd-user-unit]          packaged user unit installed                                    ok
  PASS  [unit-enabled-for-test]      MCP service enabled + running (deploy's enable --now)           ok
  PASS  [etc-charly-config]          /etc/charly/charly.yml shipped                                  ok
  PASS  [etc-charly-validates]       charly box validate -C /etc/charly                             exit=0
  PASS  [mcp-port-reachable]         forwarded port reachable from the host                          reachable=true
  PASS  [mcp-charly-ping]            MCP server answers ping                                         ok
  PASS  [mcp-charly-list-tools]      MCP server exposes the charly tools (agent.delegate, ...)       ok
  PASS  [mcp-charly-call-version]    version tool round-trips a CalVer (2026.250.0854)               ok
  PASS  [mcp-charly-call-list-boxes] box.list.boxes resolves /etc/charly locally (no fallback)       ok
  PASS  [plan:vm:cachyos-vm:10..15]  charly version / gocryptfs / socat / virsh / qemu / doctor       exit=0/ok x6
  PASS  [charly-binary]              command -v charly                                              exit=0
  SKIP  [write-bind-dropin]          verify-only mode (mutating step — runs at deploy)               as designed
  SKIP  [restart-mcp]                verify-only mode (mutating step — runs at deploy)               as designed
  PASS  [charly-mcp-unit-present]    packaged charly-mcp unit installed                               ok
  PASS  [bind-dropin-present]        all-interfaces drop-in override present                          ok
21 steps: 19 passed, 0 failed, 2 skipped (both skips are the designed verify-only mutating steps)
```

**Clean-log confirmation (R10):** `grep -c` on deploy-add.log AND update.log → `Permission denied: 0`, `here-document: 0`, `CHARLY_ACT_EOF: 0` — the drop-in's root `command:` step eliminated the guest-user EACCES + the unterminated here-doc entirely (pre-fix runs carried both).

The FULL P4 round trip is proven: the mcp: verb resolves the VM venue from the check env (plugin-check threading + plugin-mcp env fallback), dials the host-forwarded port, and the packaged charly runs `mcp serve` INSIDE the VM with the drop-in override — ping/list-tools/call-version/call-list-boxes all pass on the host side.

**R1 (the drop-in write's warning bytes, RCA'd + fixed):** the earlier runs' deploy-add/update logs carried two `bash: here-document ... CHARLY_ACT_EOF` warnings + a `mkdir ... Permission denied` — RCA: THE drop-in was written by a `run: file` step whose provision script (plugin-file's `mkdir -p $(dirname) && cat > ... <<'CHARLY_ACT_EOF'`) executed as the GUEST user on the VM (mkdir on /etc/systemd/system → EACCES); the `&&` short-circuit left the here-doc unterminated (the EOT noise). FIX: `write-bind-dropin` now writes the drop-in via a root `command:` step (`install -d` + `printf` — no here-doc, runs in the deploy's privileged channel — the same channel the `systemctl restart` step and the enable demonstrably use). The latest run (calver 2026.250.12xx) shows zero EOT/EACCES bytes; `bind-dropin-present` passes in both live phases.

## Rulebook compliance

- **R0:** check/vm/layer skills + the plan loaded.
- **R1:** the bed's earlier failures were RCA'd at every layer (published packages → the venue resolution → the drop-in compile drop → the addr var thread) and each was fixed by its owning PR (plugin-check #35, sdk #238, this bed).
- **R2:** the named batch: plugin-check #35 (the VM-venue env threading) + sdk #238 (the packaged-overrides drop-in compile fix, MERGED) + charly #559 (the plugin-mcp v2026.250.0721 weld, so the shipped plugin set carries the env fallback).
- **R3:** one bed; the enable candy + the deploy plan's drop-in write is the single start mechanism.
- **R4:** no workarounds — proper verbs + the charly-native service declaration + the deterministic addr probe.
- **R6:** worktree, PR-only.
- **R7/B12:** the bed's plan steps fail without the shipped units; the ADE gate.
- **R10:** the fresh-rebuild bed run executed + passed (the changed paths live in the matrix above).

## Change classification

- **Change class:** config (check bed + vm entity declarations + the enable candy).
- **Verification gate:** the fresh-rebuild R10 bed run — EXIT 0, 19 passed / 0 failed / 2 designed skips, summary ok:true errors:0 warnings:0.
- **Attribution tier:** analysed on a live system — the fix stack verified by the executed bed; the matrix above is the changed-path execution.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*