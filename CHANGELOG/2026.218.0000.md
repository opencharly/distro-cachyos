# 2026.218.0000 — charly bundle→fleet in box/cachyos

The live run/check steps now execute `charly fleet from-box --help` and
`charly fleet --help` (the renamed CLI). Validated with `charly box validate`.
Placeholder CalVer — the pr-validator finalizes it.
