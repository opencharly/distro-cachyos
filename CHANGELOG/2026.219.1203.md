# 2026.219.1203 — charly bundle→fleet in box/cachyos

The live run/check steps now execute `charly fleet from-box --help` and
`charly fleet --help` (the renamed CLI). Validated with `charly box validate`
and the `check-charly-selftest-pod` / `check-cachyos-gpu-vm` beds (the
selftest-pod bed's pre-existing `charly config` deploy:pod defect is routed to
the bed-robustness batch; the gpu-vm bed skips on GPU-less hosts).
