# 2026.243.1430 — feat(bed): check-cardwire-vm — VFIO NVIDIA + SPICE-terminal R10 bed (plugin-cardwire full GPU tier)

## Summary

Adds the canonical **check-cardwire-vm** R10 bed to distro-cachyos: a thin variant on the proven `cachyos-gpu-vm` substrate (auto-allocated NVIDIA hostdev via `requires_exclusive: [nvidia-gpu]`, SPICE socket + virtio head inherited) with:

- `candy/cardwire-seat` — the SPICE terminal: a sway compositor session rendering a ghostty terminal on the virtio head (host-side plugin-spice probes drive it) + seatd.
- cardwire installed from the OGC pacman repo with defaults (`cardwire-install` candy, pinned v2026.243.1202) + plugin-bpf candy (T1 facts), nvidia-driver for the in-guest GPU.
- **13 checks**: T1 (bpf LSM active, cardwired active, CLI, config defaults, seat up) / T2 (NVIDIA 0x10de visible, block target, cardwire's **real upstream mode/block gate contracts** on this 2-GPU topology, toml config persistence across daemon restart) / SPICE terminal (status, non-uniform 1280x800 screenshot, 16-char type).

## How tested

**Full R10 PASS (steps=9) on the reference CachyOS VFIO host** (RTX 4080 SUPER 01:00.0/01:00.1, IOMMU amd):

```
$ CHARLY_SKIP_FRESHNESS_CHECK=1 /tmp/charly-new -C . check run check-cardwire-vm
charly check run check-cardwire-vm: [vm-build] PASS after 4m40.914s
charly check run check-cardwire-vm: [vm-create] PASS after 4.689s
charly check run check-cardwire-vm: [deploy-add] PASS after 2m13.657s
charly check run check-cardwire-vm: [bring-up-members] PASS after 0s
charly check run check-cardwire-vm: [check-live] PASS after 17.593s
charly check run check-cardwire-vm: [update] PASS after 2m2.175s
charly check run check-cardwire-vm: [cleanup] PASS after 5.719s
charly check run check-cardwire-vm: PASS (steps=9)
```

Per-step matrix (check-live, 13/13):

```
PASS  [t1-bpf-lsm-active]      guest bpf LSM active (the eBPF blocker's gate)      exit=0
PASS  [t1-cardwired-active]     cardwired is active after install                    exit=0
PASS  [t1-cardwire-cli]         cardwire CLI at /usr/bin/cardwire                     ok
PASS  [t1-cardwire-config-defaults] experimental_nvidia_block=true default present    ok
PASS  [t1-seat-up]              sway compositor (SPICE terminal) up                  exit=0
PASS  [t2-nvidia-visible]       passed-through NVIDIA GPU in-guest (0x10de)          exit=0
PASS  [t2-nvidia-nondefault]    cardwire exposes the NVIDIA block target             exit=0
PASS  [t2-mode-gate]            set manual REFUSED (2-GPU Laptop classification)     exit=0
PASS  [t2-block-surface-gate]   per-GPU block REFUSED (manual-mode-only); flag clear exit=0
PASS  [t2-mode-persistence]     auto-apply-gpu-state survives cardwired restart      exit=0
PASS  [spice-connected]         SPICE wire connected                                 SPICE: ok
PASS  [spice-renders]           screenshot 1280x800 non-uniform (rendered seat)      saved
PASS  [spice-terminal-type]     keystrokes reach the ghostty terminal                typed 16 chars
```

SPICE screenshot artifact (the SPICE-terminal render, 26KB): `/tmp/cardwire-seat.png`; full check-live log at `.check/check-cardwire-vm/2026.243.1416/check-live.log` (evidence copy: /tmp/cardwire-spike/evidence/r10-vm/).

**Upstream-contract adaptations (R1, documented):** the plan's T2 assumed `set manual -> gpu block -> ENOENT` on the VM. Live runs exposed cardwire 0.12.1's real gates on this 2-GPU virtio+NVIDIA topology: the daemon classifies it **Laptop** (default non-discrete + discrete dGPU) → `set manual` refused (Desktop/1-GPU/3+-GPU only) → per-GPU `--block` refused (manual-mode-only). The bed now **asserts those refusals as the contract** + proves the flag/persistence surfaces; the block→ENOENT→unblock→open-OK semantics remain proven by the Phase-0 spike (Manual-classified guest, SPIKE-REPORT.md §0.2, all PASS). No workarounds; the gates are upstream design.

## Rulebook compliance

- R1: every failure RCA'd (store split → engine.rootful; tmpfs disk → TMPDIR+STATE_DIR on /; CDI hook → toolkit-less bed; home .config ownership → seat candy chown; mode/block gates → upstream contract).
- R3: reuses the cachyos-gpu-vm substrate + existing candies; no duplicated VM entity.
- R4: zero workarounds in the final bed.
- R7: full R10 pasted above, run on the committed tree config.
- R10: fresh disposable proof on the reference host; pins are merged tags (plugin-cardwire v2026.243.1202, plugin-bpf v2026.243.1059).

## Change classification

- Change class: new capability (canonical VFIO/SPICE R10 bed).
- Verification gate: full R10 PASS (steps=9, 13/13) on the reference host, pasted above.
- Attribution tier: `fully tested and validated` — both new paths (bed + seat candy) executed live on the reference host.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*