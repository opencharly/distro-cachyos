# 2026.251.1511 — fix(check): the E-5 appium session instrument fixture + the E-5 wave dependency pins — evidence gate CLOSED (run 2026.251.1321, exit 0)

## Summary

Cutover E's **E-5 evidence gate is CLOSED**: the check-android-emulator-pod R10 bed ran end-to-end with the E-5 appium session instrument — recorder bracket open → live Appium session → pulled MP4 → transcode pipeline word — **exit 0, ok:true, 15/15 phases** (run calver **2026.251.1321**).

This PR lands the E-5 fixture wave on the cachyos box:

**The instrument fixture (all commits):**
- **b886f0c** — feat(check): the E-5 appium session instrument on check-android-emulator-pod (record via startRecordingScreen/stopRecordingScreen under the runner-owned recorder bracket, evidence row lands the MP4).
- **d08fbeb** — fix(check): the E-5 appium lifecycle steps are check: verbs — check-live runs verify-only (E-5 R1; the runner's live bracket is the only session writer).
- **4120223** — fix(check): E-5 F-Droid via the committed canonical client — the apkpure package: source is Cloudflare-bot-walled (R1 egress; the committed client is the deterministic path).
- **2e93772** — fix(check): E-5 stop-race — drop the in-plan appium session-stop/session-delete steps (one identity, one bracket).
- **9910a54** — fix(check): E-5 session caps — newCommandTimeout 3600s so the recorder's stops land on a live session.
- **82f0737** — fix(check): E-5 single identity — the fixture OWNS the session lifecycle (cross-section overlay: keyed create aliases the live shared session, baked delete neutralized as a same-id host-side no-op).
- **3c3a0af / 823b4aa** — fix(check): the appium-session-delete neutralizer — the verb-less skip:true form was a load-time error (R1 run 2026.251.0937); the same-id no-op keeps ONE identity, PASSES, and the runner finalize still lands stop_recording_screen on the live fixture session.
- **0fd851d** — fix(check): status-shows-android/nested drive the ACTIVE charly via the CHARLY_BIN check-var (R1 run 2026.251.0955 — exit=1 came from the host PATH charly (v2026.248.2159, max schema 2026.248.1030) failing to resolve the 2026.249.2125 project; the var pins the runner binary, the canonical host-side check-var).
- **b375e4b** — fix(check): layer-android-sdk pin → v2026.250.1813 (the 239.1604 pin baked the OLD aapt2 check, failing check-image; matches the layer's own require).
- **53a97ad** — merge: catch up with main (sweep pin fixes #76-#79).

**The E-5 wave dependency pins (this PR, after the three component PRs merged + tagged today):**
- plugin-appium **v2026.251.1353** — the recorder endpoint re-resolve + bracket hold + stale-clear + session_file key + keyed aliasing (E-5 PR #7, merged).
- plugin-media **v2026.251.1349** — the transcode output-track-timescale pin (E-5 PR #3, merged).
- pod-android-emulator-layer **v2026.251.1403** — the baked av-suite session_file: baked scoping (E-5 PR #9, merged).
- pod-immich **v2026.251.1422** (companion pod-postgresql pin PR #5, merged) + layer-vectorchord **v2026.251.1508** (companion pin PR #7, merged) — the last two v2026.239.1627 pod-postgresql requesters in the cachyos closure.

**Also in-tree (R1, the B5-routed naming):** pod-postgresql v2026.239.1627 → **v2026.247.1304** in box/immich-ml + candy/charly-marketplace — the v2026.239.x stale-pin class (same family the android sweep fixed): every check bed run warns "resolved to multiple versions" because these cachyos candies still request the old tag while the resolver picks the newest. Fixed here for the cachyos closure (in-tree 2c4d22d) with the two companion repo PRs (pod-immich v2026.251.1422, layer-vectorchord — this wave); the remaining copies (charly box/fedora, distro-fedora) ride the named umbrella pod-postgresql pin batch (parent wave, same cutover pattern as the android sweep). Verified: this run's resolution at ZERO non-allowlisted warnings.

## How tested

### The E-5 R10 bed — FINAL-HEAD run 2026.251.1443 (exit 0, ZERO non-allowlisted warnings)

charly check run bed **check-android-emulator-pod**, calver **2026.251.1443** — the final head with ALL wave pins + the pod-postgresql alignment in tree (composed via CHARLY_REPO_OVERRIDE for the branch + the two companion pod-postgresql PRs): **exit 0, ok: true, 15/15 phases.** Artifacts retained: distro-cachyos/.check/check-android-emulator-pod/2026.251.1443/.

**Phase matrix — 15/15 ok:true:**

image-build          ✓  (1m43s, 0 errors, 518 cache hits)
check-image          ✓  (50s, 0 errors)
deploy-add           ✓  (17s, 0 errors)
config               ✓  (7s, 0 errors)
start                ✓  (4s, 0 errors)
bring-up-members     ✓  (48s)
check-live           ✓  (2m56s, 0 errors — 325 steps: 302 passed, 0 failed, 23 skipped)
feature-run          ✓  (2m34s, 0 errors — 317 steps: 294 passed, 0 failed, 23 skipped)
rebuild-members-down ✓  (6s)
update               ✓  (58s, 0 errors)
re-bring-up-members  ✓  (37s)
check-live-rebuild   ✓  (2m48s, 0 errors — 325 steps: 302 passed, 0 failed, 23 skipped)
feature-run-rebuild  ✓  (0 errors — 317 steps: 294 passed, 0 failed, 23 skipped)
cleanup              ✓
cleanup-members      ✓  (42s)

Run diagnostics: **errors: 0, ZERO non-allowlisted warnings** (the only warnings, 17, are the allowlisted dev-worktree-binary-notice; the pod-postgresql multi-version notices are GONE — the 2c4d22d in-tree fix + the two companion pins verified by this run). 15/15 phases ok:true.

**evidence.yml (the E-5 gate rows):**

    evidence:
      - instrument: check-android-emulator-pod.appium-screen
        origin: session
        verb: appium
        venue: check-android-emulator-pod
        phase: live
        segment:
          - { bytes: 87153918, phase: "live", start: "2026-09-08T14:46:59Z", stop: "2026-09-08T14:53:17Z" }
        artifact:
          - { kind: "mp4", path: ".check/check-android-emulator-pod/2026.251.1443/media/appium-1.mp4" }
        pipeline:
          - plugin: transcode
            plugin_input: { artifact: "/tmp/charly-appium-screen.mp4", to: "mp4" }

**Segment + artifact:** recorder bracket (state under capture/check-android-emulator-pod.appium-screen/): row.json segment live session **173e76e7-3cef-4139-81ac-c95a7b9204a5** (start 14:50:41Z / stop 14:53:17Z); media/appium-1.mp4 — **87,153,918 bytes** (87MB), h264 1080x2424, ffprobe duration 150.801833s, landed by the transcode pipeline row.

**Key live rows (feature-run / check-live-rebuild logs):**

    PASS  check appium=session-create  [appium-session-create]  173e76e7-3cef-4139-81ac-c95a7b9204a5
    PASS  check the running Appium server reports itself ready ...  [appium-status-ready]  {"value":{"ready":true,...,"build":{"version":"3.7.0"}}}
    PASS  check appium=screenshot  [appium-screenshot]  wrote 100728 bytes to /tmp/charly-check-appium-screenshot.png
    PASS  check appium=session-delete  [appium-session-delete]  deleted (alias — the shared fixture session stays live)
    PASS  check neutralize the baked appium-session-delete (E-5 ONE identity, ONE bracket ...)  [appium-session-delete]  exit=0
    317 steps: 294 passed, 0 failed, 23 skipped   (feature-run / feature-run-rebuild)
    325 steps: 302 passed, 0 failed, 23 skipped   (check-live / check-live-rebuild)

## Rulebook compliance

- R1 — every fixture commit carries its RCA (referenced run evidence in the commit messages); the pod-postgresql divergence is root-caused (v2026.239.x stale-pin class), fixed in-tree (2c4d22d) + via the two companion pins, and PROVEN at zero non-allowlisted warnings by run 2026.251.1443.
- R3 — one session lifecycle per file key (plugin-appium's session_file is the single implementation; this bed is the consumer).
- R4 — no workarounds: the CHARLY_BIN check-var is the canonical runner-binary pin; no sleeps/retries; no hand-pins (all pins are merged tags).
- R6 — no force-push, no direct push to main; feat/ branch + PR-only landing; all pins are merged refs.

## Change classification

- **Change class**: check-bed fixture + pin bump (Cutover E — the E-5 evidence gate close).
- **Verification gate**: the full R10 bed run 2026.251.1443 on the final head (exit 0, ok:true, 15/15 phases, ZERO non-allowlisted warnings, evidence rows + mp4 pasted above) — the last E-unit R10 of Cutover E, CLOSED.
- **Attribution tier**: fully tested and validated — the fixture + instrument + transcode pipeline ran live to exit 0 with retained output (evidence above); the three dependency tags are merged and pinned.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*