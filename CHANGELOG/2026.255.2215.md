# 2026.255.2215 — refs: bump pod-immich-ml to v2026.255.1229 (the pod-immich-ml pair; clears the transitive pod-postgresql skew)

## Summary

**2 files / 2 lines — the `pod-immich-ml` pair**, moved at BOTH of its declaration sites:

| changed line | pin | from -> to |
|---|---|---|
| `box/immich-ml/charly.yml:21` | pod-immich-ml | v2026.243.0516 -> v2026.255.1229 |
| `candy/charly-marketplace/charly.yml:175` | pod-immich-ml | v2026.243.0516 -> v2026.255.1229 |

This is the in-tree fix for the repo's transitive `pod-postgresql` version-mismatch notice (the hop-by-hop
RCA is below). The composing bed that RESOLVES and RUNS the changed pin — `check-cachyos-immich-ml-pod` —
is green at this head, with errors 0 and warnings 0 (Gate 5).

**The criterion, in the reviewer's own words** (validator review of the earlier, larger shape at head
`4d10050`): *"carry only the lines proven by a green composing bed (the pod-immich-ml pair)"*. That is
exactly what this change is. `candy/charly-marketplace/charly.yml:175` is the DECLARATION CLOSURE of the
same version — no bed in this repo composes that candy (the sweep is pasted at Gate 4), and the version
beneath it is the one Gate 5's bed runs — which is why the two travel together as a pair.

**What is NOT in this change, and why — facts, not a waiver.** The other pin lines this work had gathered
are deliberately absent: the four `layer-container-nesting` lines *including both box sites*
(`box/charly-selftest:9`, `box/crabbox-e2e:10`, `box/githubrunner:18`,
`candy/charly-marketplace/charly.yml:184`), the two `plugin-spice` sites, `layer-asciinema` and
`layer-charly-build`. They live in **opencharly/distro-cachyos#86** (draft, branch
`feat/pins-gated-followup`) with **#85** as the tracker issue (each line, its composing bed, the verbatim
obstacle, the owner, the next action). Two validator rulings put them there, and both are on the record
rather than rewritten:

- the CURRENT ruling (review at `4d10050`) — *"carry only the lines proven by a green composing bed (the
  pod-immich-ml pair), and move all four layer-container-nesting lines — including both box sites — into
  the same follow-up that holds the other gated pins; or land the owner fixes (distro-cachyos#83,
  opencharly/charly#595) in this tree so every carried line has a green bed"*;
- the EARLIER ruling (reviews at `ca7754e9` / `2b257e71`) — that dropping the two box
  `layer-container-nesting` sites re-opens the repo-wide alignment the sweep exists to close.

The measurable cost of the current ruling is stated at Gate 3: `charly box reconcile --dry-run` lists
SEVEN outstanding rewrites (the four `layer-container-nesting` sites, the two `plugin-spice` sites, the
`layer-asciinema` site). That is BY DESIGN in this shape and it is what #86/#85 exist to close.

**No exception is claimed and no waiver is asserted.** Every line in this diff is exercised by a green
composing bed at this head. For the record only: a maintainer authorization comment exists on the earlier
shape (…/pull/82#issuecomment-5648952002); the validator ruled that a comment grants nothing under the
co-equal independence clause, and this change does not need it — that comment could be deleted without
changing a single line of evidence here.

**Change class:** cross-repo ref/pin bump, configuration only (2 files, 2 lines).
**Head under review:** `c608ff6` — the branch is ONE commit on top of current `origin/main` (`ba36901`):
`git rev-list --count origin/main..HEAD` = **1** (Gate 0), and the merge is a SQUASH, so `main` receives
exactly one commit carrying one change. There are no split/restore cycles and no evidence-only commits on
this branch.

## The transitive `pod-postgresql` warning — RCA, and the in-tree fix this branch carries

The one warning this repo's `charly box validate` reports on pristine `main` (`ba36901`) is transitive —
neither declaration of the offending candy is in this repo:

    $ cd /tmp/dc-main && charly box validate
    warning: Warning: candy github.com/opencharly/pod-postgresql resolved to multiple versions; using newest 2026.247.0100 (from github.com/opencharly/pod-postgresql@v2026.247.1304), ignoring 2026.144.1531 (from github.com/opencharly/pod-postgresql@v2026.239.1627)
    charly box validate: OK — checked 506 candies, 40 boxes, 46 deploys, 7 distros, 7 builders; 1 warning, 0 errors
    rc=0

**Every hop executed, not read** — the stale candidate enters ONLY through this repo's own `pod-immich-ml`
pin:

    $ git grep -n pod-immich -- box/immich-ml/charly.yml candy/charly-marketplace/charly.yml
    box/immich-ml/charly.yml:20:            - '@github.com/opencharly/pod-immich:v2026.251.1422'
    box/immich-ml/charly.yml:21:            - '@github.com/opencharly/pod-immich-ml:v2026.255.1229'
    candy/charly-marketplace/charly.yml:162:            - '@github.com/opencharly/pod-immich:v2026.251.1422'
    candy/charly-marketplace/charly.yml:175:            - '@github.com/opencharly/pod-immich-ml:v2026.255.1229'
    $ gh api 'repos/opencharly/pod-immich-ml/contents/charly.yml?ref=v2026.243.0516' -q .content | base64 -d | grep -n pod-immich
    15:            - '@github.com/opencharly/pod-immich:v2026.243.0410'      # the STALE require, at the pin this PR moves AWAY from
    $ gh api 'repos/opencharly/pod-immich/contents/charly.yml?ref=v2026.243.0410' -q .content | base64 -d | grep -n pod-postgresql
    18:            - '@github.com/opencharly/pod-postgresql:v2026.239.1627'   # -> candy 2026.144.1531
    $ gh api 'repos/opencharly/pod-immich/contents/charly.yml?ref=v2026.251.1422' -q .content | base64 -d | grep -n pod-postgresql
    18:            - '@github.com/opencharly/pod-postgresql:v2026.247.1304'   # -> candy 2026.247.0100 (the newest)

Chain: `pod-immich-ml@v2026.243.0516` -> `pod-immich@v2026.243.0410` ->
`pod-postgresql@v2026.239.1627` (candy `2026.144.1531`), while this repo's DIRECT pins already resolve
`pod-postgresql` to the newest candy (`2026.247.0100`, via `v2026.247.1304`) — hence the two versions and
the warning.

**The fix is in-tree, and this branch carries it.** opencharly/pod-immich-ml#5 replaced that require with
`pod-immich@v2026.251.1422`, MERGED, and its tag-on-merge cut the tag this branch pins:

    $ gh pr view 5 -R opencharly/pod-immich-ml --json state,mergedAt,mergeCommit,headRefOid -q '.state + " " + .mergedAt + " " + .mergeCommit.oid + " " + .headRefOid'
    MERGED 2026-09-12T12:29:51Z dc0d5333003d25977dafa71d529499a081d4fed8 8e38c37131a06406fea0542aba2f96339e78bb85
    $ gh api repos/opencharly/pod-immich-ml/tags -q '.[0:3][].name'
    v2026.255.1229
    v2026.243.0516
    v2026.239.1604
    $ gh api 'repos/opencharly/pod-immich-ml/contents/charly.yml?ref=v2026.255.1229' -q .content | base64 -d | grep -n pod-immich
    15:            - '@github.com/opencharly/pod-immich:v2026.251.1422'      # the newest require, i.e. ONE pod-postgresql version

The pin chosen is a MERGED tag (umbrella rule 5: never a PR branch). The warning's disappearance in-tree
is the `1 warning -> 0 warnings` transition between Gate 1's two runs.

## How tested — verbatim, all on THIS branch

### Gate 0 — head, tree, atomicity

    $ git -C /tmp/dc82 log --oneline -1
    c608ff6 refs: bump pod-immich-ml to v2026.255.1229 at both declaration sites
    $ git -C /tmp/dc82 rev-list --count origin/main..HEAD
    1
    $ git -C /tmp/dc82 diff --stat origin/main..HEAD
     box/immich-ml/charly.yml            | 2 +-
     candy/charly-marketplace/charly.yml | 2 +-
     2 files changed, 2 insertions(+), 2 deletions(-)

### Gate 1 — `charly box validate`: the whole closure, on both trees

    $ cd /tmp/dc82 && charly box validate       # this branch (c608ff6)
    charly box validate: OK — checked 506 candies, 40 boxes, 46 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
    rc=0
    $ cd /tmp/dc-main && charly box validate    # pristine origin/main (ba36901): the DISCRIMINATOR
    warning: Warning: candy github.com/opencharly/pod-postgresql resolved to multiple versions; using newest 2026.247.0100 (from github.com/opencharly/pod-postgresql@v2026.247.1304), ignoring 2026.144.1531 (from github.com/opencharly/pod-postgresql@v2026.239.1627)
    charly box validate: OK — checked 506 candies, 40 boxes, 46 deploys, 7 distros, 7 builders; 1 warning, 0 errors
    rc=0

Same verb, same binary, two trees; the only delta is the 2 lines of this diff.

### Gate 2 — the real YAML parse of every changed file

    $ python3 -c "import yaml,sys; [yaml.safe_load(open(p)) for p in sys.argv[1:]]" \
        box/immich-ml/charly.yml candy/charly-marketplace/charly.yml
    (no output, rc 0)

### Gate 3 — `charly box reconcile --dry-run`: what this change does NOT close (by design)

    $ cd /tmp/dc82 && charly box reconcile --dry-run
    github.com/opencharly/layer-asciinema -> v2026.246.1622 (was at 2 versions)
    github.com/opencharly/layer-container-nesting -> v2026.247.0157 (was at 2 versions)
    github.com/opencharly/plugin-spice -> v2026.248.2114 (was at 2 versions)
    would rewrite 7 pin(s):
      charly.yml: @github.com/opencharly/layer-container-nesting:v2026.239.1647 -> @github.com/opencharly/layer-container-nesting:v2026.247.0157
      charly.yml: @github.com/opencharly/layer-container-nesting:v2026.239.1647 -> @github.com/opencharly/layer-container-nesting:v2026.247.0157
      charly.yml: @github.com/opencharly/layer-container-nesting:v2026.239.1647 -> @github.com/opencharly/layer-container-nesting:v2026.247.0157
      charly.yml: @github.com/opencharly/plugin-spice/candy/plugin-spice:v2026.237.1428 -> v2026.248.2114
      charly.yml: @github.com/opencharly/plugin-spice/candy/plugin-spice:v2026.237.1428 -> v2026.248.2114
      charly.yml: @github.com/opencharly/layer-asciinema:v2026.240.0119 -> v2026.246.1622
      charly.yml: @github.com/opencharly/layer-container-nesting:v2026.239.1647 -> @github.com/opencharly/layer-container-nesting:v2026.247.0157
    rc=0

Those seven rewrites are exactly the eight lines #86 carries and #85 tracks (the sites show as
`charly.yml: ` because the verb prints the basename). `pod-immich-ml` is NOT among them: the pair this
PR moves was its only divergence, and it is closed here.

### Gate 4 — the sweep: no bed in this repo composes `candy/charly-marketplace`

    $ git grep -n 'charly-marketplace' -- '*.yml'
    candy/charly-marketplace/charly.yml:1:charly-marketplace:
    candy/charly-marketplace/charly.yml:10:            That is `candy/charly-marketplace/charly.yml` in opencharly/marketplace, which
    $ git grep -n 'charly-marketplace' -- '*.yml' | grep -v '^candy/charly-marketplace/charly.yml' | wc -l
    0

Both matches are inside the candy's own file (its entity key and a prose mention). So for
`candy/charly-marketplace/charly.yml:175` the DECLARATION is graded by the reconcile gate (Gate 3 — the
pair is one version repo-wide) and the VERSION is graded by Gate 5's bed; that is the closure-pair
criterion, stated rather than implied.

### Gate 5 — the composing bed, run at THIS head (R10)

| evidence | value |
|---|---|
| bed | `check-cachyos-immich-ml-pod` — the only bed in this repo that resolves and runs `pod-immich-ml` (`box/immich-ml`) |
| tree | `/tmp/dc82` on THIS branch (`c608ff6`): the pins exactly as this PR leaves them |
| vehicle | `/tmp/cpinbump/bin/charly` (charly 2026.255.1803, plugin-build v0.2026255.1833) — the propagated build; pins in-tree, NO override |
| calver | **2026.255.2205** |
| result | **PASS** (`ok: true`), steps=14, **errors 0**, **warnings 0**, allowlisted 15 |
| timing / cache | 289 s total (image-build 86 s), cache **316/336** |
| the class this PR clears | `grep -c pod-postgresql image-build.log` = 5, and all five are **OCI LABEL values** (`ai.opencharly.service.supervisord`, `.service`, `.candy_version`, …), not diagnostics: the run contains NO `resolved to multiple versions` line anywhere |
| summary path | `.check/check-cachyos-immich-ml-pod/2026.255.2205/summary.yml` |

The operator's RTX is this host's desktop display, so charly's preempt path refused the vfio switch for
the external GPU client and the bed PASSED anyway — verbatim:

    preempt: could not set "nvidia-gpu" to vfio mode after releasing "check-cachyos-immich-ml-pod":
    switch-to-vfio REFUSED: nvidia still held by an external GPU client (the holding process could not be
    identified). charly auto-preempts its own GPU pods; close these external GPU clients and retry
    (refusing to force-unbind — would wedge the device_lock)

No unbind was forced, and nothing holding `/dev/dri` was touched.

## Rulebook compliance

| Rule | How |
|---|---|
| R0 — skills first | git-workflow / check-bed / vm skill contexts loaded before the first action. |
| R1 — RCA every anomaly | The one warning this repo emitted on `main` is root-caused hop by hop to the pin this branch moves, and cleared in-tree — proven by the 1-warning -> 0-warning transition between two `charly box validate` runs on the same binary (Gate 1) and by the bed run (Gate 5). Nothing is parked as "pre-existing" or routed away. |
| R2 — no split batches | One change, one PR, no half-batch: the pair is the reviewers' own named subset ("carry only the lines proven by a green composing bed (the pod-immich-ml pair)"), and the work that is not in it is tracked as one artifact (#86) plus one tracker issue (#85), measured by Gate 3. No line in this diff is carried with a red or missing gate. |
| R3 — no duplication | Each pin edited at its declaration site only; no parallel mechanism introduced. |
| R4 — no workarounds | No sleeps, no blind retries, no hand-pins: the pin is a merged tag, and the "fix" is the upstream require correction propagated through that tag. |
| R5 — delete legacy | N/A — no path, name or mechanism is superseded. |
| R6 — git safety | The new branch is ONE commit on top of current `origin/main` (`git rev-list --count` = 1); no amend, no force-push, no rebase, no hook bypass, no direct push to main. The superseded PR #82 is closed as superseded-by-shape with both rulings cited; its branch is left untouched. |
| R7 — prove the gate | `charly box validate` rc 0 with **0 warnings, 0 errors** at this head, against the 1-warning baseline on `main` (Gate 1), plus the fresh composing-bed run (Gate 5). |
| R8 — no dead ends | The changed require's emission path is exercised end to end by the bed in Gate 5 (the pod image is built and run with the new pin). |
| R9 — test tools in test candy | N/A — no install/test content added. |
| R10 — fresh disposable proof | The composing bed of the changed pin was run AT this head, pins in-tree, no override (Gate 5), with errors 0 and warnings 0; the summary path is pasted. No line in this change lacks a green composing bed. |
| RDD — reality-driven design | The warning's transitive origin was established by executing every hop (Gate: the RCA above) and its removal by the falsifiable A/B on two trees with the same binary (Gate 1). |
| ADE — acceptance-driven engineering | The pin's acceptance is the resolver finding ONE pod-postgresql version and `charly box validate` reading 0 warnings — asserted, not assumed. |
| SDD — spec-driven delivery | N/A — configuration pins only; no CUE schema / generated Go touched. |

## Change classification

- **Change class:** cross-repo ref/pin bump, configuration only — **2 files, 2 lines** (the `pod-immich-ml`
  pair). The eight lines this work originally gathered are split to **#86** and tracked in **#85**, which
  Gate 3 measures.
- **Verification gate:** `charly box validate` at this head (**0 warnings, 0 errors**, against `main`'s
  1 warning) + `charly box reconcile --dry-run` listing exactly the rewrites the follow-up owns +
  the fresh composing-bed run (Gate 5).
- **Attribution tier:** analysed on a live system — the gates and the bed ran live with retained output.
  NOT claimed: any proof of the eight lines in #86 (they are the follow-up's), and any claim that the
  published opencharly.ai page moves with this repo's pin.

*Assisted-by: pi ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*