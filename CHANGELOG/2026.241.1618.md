# 2026.241.1618 — docs: no charly/candy exceptions remain in this repo

## Summary of changes

The header comment I added in **#33** says two refs still point at `charly/candy/…` — `charly` and
`charly-mcp` — held back because repointing them *then* surfaced a remote-candy-name conflict.

**#34 repointed both**, to the same tags, and `charly box validate` is exit 0. That comment is now
the only thing in this repo still claiming those exceptions exist.

Corrected to say what is true: **no exceptions remain** — and *why* the two were briefly held back,
so a reader who finds #33 in the history understands the sequence rather than wondering whether a
conflict is still lurking.

## Change class

**docs** — one header comment. No refs, no pins, no box logic.

**Closes / relates to:** finishes the thread from `#33` → `#34`; Phase D.6 of the candy
de-submodule cutover.

**Breaking change + rollback:** none — a comment. Rollback is a plain revert.

## How tested — exact gate, commands, and pasted output

- **The gate for this change class:** `charly box validate` on the final tree, plus a sweep proving
  the claim the comment now makes.
- **Eval beds run:** `N/A (docs)`.
- **Did the CHANGED code path execute live?** `N/A` — a comment. The claim it makes is verified below.
- **Concurrency:** `N/A`.

*1. Gate output* (zero warnings):
```
$ charly box validate
exit=0
                          # zero non-noise output lines
```

*2. The claim the comment now makes, verified:*
```
$ grep -n 'charly/candy' charly.yml
                          # no matches — the comment itself was the last one

$ grep -rhoE '@github\.com/opencharly/charly/candy/[a-z0-9-]+' --include='*.yml' .
                          # no matches repo-wide

$ grep -rn 'layer-charly:\|pod-charly-mcp:' --include='*.yml' . | head -3
box/githubrunner/charly.yml:16:    - '@github.com/opencharly/layer-charly:v2026.241.1407'
box/charly-selftest/charly.yml:8:  - '@github.com/opencharly/pod-charly-mcp:v2026.241.1219'
candy/charly-marketplace/charly.yml:17: - '@github.com/opencharly/layer-charly:v2026.241.1407'
```

*3. Docs-only evidence:*
```
$ git show --stat HEAD
 charly.yml | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

A comment block. No ref, no pin, no plan step, no CI.
```

## Attribution tier

**documentation reviewed** — a docs-only change class, which the rulebook caps at this tier. The
claim was verified against the tree (block 2), but no code path ships here.

## Harness rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow` loaded before the first git call.
- **R1 RCA + zero warnings:** the stale text was found by re-running my own sweep after #34 landed,
  rather than assuming my earlier PR's comment still held. Zero warnings.
- **R2 no out-of-scope:** only the stale sentence is touched.
- **R3 no duplication:** one statement of the ref convention, in the one header that carries it.
- **R4 no workaround:** `N/A`.
- **R5 delete legacy completely:** the false claim is replaced, not left with a caveat appended —
  and the reason the exceptions briefly existed is kept, because deleting that would make #33
  unreadable in hindsight.
- **R6 git safety:** fresh clone, branch from `main`, only `charly.yml` staged by name,
  `git show --stat` verified after commit. No force-push, no `--no-verify`, no push to `main`.
- **R7 runtime gate:** `charly box validate` run on the final tree.
- **R8 / R9:** `N/A`.
- **R10 disposable + coverage:** `N/A` for a comment; the sweep in block 2 is the check.
- **RDD / ADE / SDD:** RDD — the claim was verified by sweeping the repo, not by memory of what #34
  was supposed to do.
- **Hard cutover:** ONE atomic commit.
- **Kernel/plugin boundary law / Disposable-only autonomy:** `N/A`.
- **Clean architecture + Go gates:** `N/A — no Go source in this repo.`
- **CHANGELOG:** `N/A` — `tag-on-merge` writes it from this body at merge.

---

*Assisted-by: Claude Code claude-opus-5[1m] (documentation reviewed)*