# 2026.234.0809 — chore: re-stamp schema version to 2026.232.0520 (spec strict-cleanup cutover)

## Summary

Re-stamps the schema `version:` from `2026.225.1508` to `2026.232.0520` (the spec strict-cleanup cutover's HEAD, opencharly/charly#371), produced by running the charly 2026.232.0520 binary's `charly migrate`. The config carries no `install_template`, so the install-template-to-phases reshaper is a no-op.

## How tested — pasted output

```
$ charly migrate
migrated to schema 2026.232.0520

$ charly box validate ; echo exit=$?
exit=0
```

**Disposable targets + fresh rebuild (R10 beds with the changed binary).** The charly main-repo cutover ran its four class beds on explicit `disposable: true` targets — each a full build → deploy → fresh-update → live-check sequence:

```
$ charly -C box/fedora check run check-pod
charly check run check-pod: PASS (steps=13)      # summary: box/fedora/.check/check-pod/2026.234.0638/summary.yml
$ charly check run check-k3s-vm
charly check run check-k3s-vm: PASS (steps=9)    # .check/check-k3s-vm/2026.234.0646/summary.yml
$ charly check run check-charly-vm
charly check run check-charly-vm: PASS (steps=9) # .check/check-charly-vm/2026.234.0648/summary.yml
$ charly check run check-local-vm
charly check run check-local-vm: PASS (steps=9)  # .check/check-local-vm/2026.234.0700/summary.yml
```

Every bed: all steps `ok: true`, `errors: 0`; check-pod `warnings: 0`; the VM beds' guest-side pacman `pacnew`/`--needed` messages are on the repo's standing allowlist (`pacman-needed-package-already-current`) — zero non-allowlisted warnings. The fixture-test suite (this box.s charly.yml is loaded as a live fixture — the charly suite has exactly two packages):

```
$ cd charly && go test ./...
ok  github.com/opencharly/charly/charly  69.081s
ok  github.com/opencharly/charly/charly/internal/pluginsgen  0.007s
```

## Project-rulebook rule-compliance

- Part of the spec strict-cleanup schema cutover (opencharly/charly#371, merged): this is the box leg re-stamping the config to the new HEAD; the main repo pins this head.
- Every box landing carries its CHANGELOG entry (placeholder CalVer, finalized at merge).
- This repo has no `go` CI gate; the only check is the validator itself, whose conclusion on this head is authoritative.

*Assisted-by: pi ollama-cloud deepseek-v4-flash:0731 (analysed on a live system)*