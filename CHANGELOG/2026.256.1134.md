# 2026.256.1134 — docs: correct cachyos box README

## Summary

Correct stale facts in the repo-root `README.md` — the cachyos box README. (The
requested `box/cachyos/README.md` does not exist in this repo; the box README is
the repo-root `README.md`, where every described stale fact lives.)

- Candy refs moved off `opencharly/charly/candy/*` into standalone repos; the
  README now cites the real
  `@github.com/opencharly/<layer-*|pod-*|plugin-*>[:subdir]:<tag>` shape pinned to
  explicit CalVer tags, not "a single tag".
- The Arch base DAG leaf is `quay.io/archlinux/archlinux:base-<ver>`, not
  `docker.io/archlinux`.
- The pacstrap renderer is `RenderPacstrapExtraConf` in
  `sdk/buildkit/build_helpers.go`, not `charly/build.go`.
- The schema-version hard-fail message is `"newer than this charly supports"`,
  not the quoted "update charly".

## How tested

Documentation-only change; no runtime gate. Every correction was verified against
the live tree and the claim-keyed sweeps were run over the whole repo.

Stale candy-ref shape and "single tag" claim — CHANGELOG history only, no live
`*.yml` refs:

```
$ grep -rn 'charly/candy' --include='*.md' .
./CHANGELOG/2026.241.1618.md:1:# 2026.241.1618 — docs: no charly/candy exceptions remain in this repo
./CHANGELOG/2026.241.1618.md:5:The header comment I added in **#33** says two refs still point at `charly/candy/…` — `charly` and
./CHANGELOG/2026.241.1618.md:41:$ grep -n 'charly/candy' charly.yml
./CHANGELOG/2026.241.1618.md:44:$ grep -rhoE '@github\.com/opencharly/charly/candy/[a-z0-9-]+' --include='*.yml' .
./CHANGELOG/2026.241.1528.md:1:# 2026.241.1528 — fix(box): repoint the last stale charly/candy/charly refs to layer-charly/pod-charly-mcp
./CHANGELOG/2026.241.1528.md:5:The charly candy de-submodule cutover moved the `charly` and `charly-mcp` candies out of the charly repo's `candy/` directory into the standalone `layer-charly` / `pod-charly-mcp` repos. Nine refs in this repo still used the pre-cutover `@github.com/opencharly/charly/candy/<name>` path, which resolves an OLD charly tag that still ships those candies — colliding with the standalone copies as `remote candy name conflict` errors in `charly box validate` (opencharly/charly#437).
./CHANGELOG/2026.241.1528.md:9:- Advance the `import: arch` pin from `distro-arch:v2026.241.0311` (pre-fix — that tag still carries the old `charly/candy/charly` refs in arch-coder / charly-arch / check-agent-box) to `distro-arch:v2026.241.1429`, the tag minted by distro-arch#31 that repointed them.
./CHANGELOG/2026.241.1436.md:5:The candy de-submodule cutover moved these candies out of `charly/candy/` into their own
./CHANGELOG/2026.241.1436.md:7:(`@github.com/opencharly/charly/candy/<name>:<charly tag>`).
./CHANGELOG/2026.241.1436.md:14:| was `charly/candy/…` | now |
./CHANGELOG/2026.241.1436.md:33:`charly` and `charly-mcp` still resolve through `charly/candy/…` — **9 refs** across 5 files (7 for
./CHANGELOG/2026.241.1436.md:94:$ grep -rn '@github.com/opencharly/charly/candy/' --include='*.yml' .
./CHANGELOG/2026.241.1436.md:95:box/githubrunner/charly.yml:16:            - '@github.com/opencharly/charly/candy/charly:v2026.240.0831'
./CHANGELOG/2026.241.1436.md:96:box/openclaw-desktop/charly.yml:21:            - '@github.com/opencharly/charly/candy/charly:v2026.240.0831'
./CHANGELOG/2026.241.1436.md:97:box/charly-selftest/charly.yml:8:            - '@github.com/opencharly/charly/candy/charly-mcp:v2026.240.0831'
./CHANGELOG/2026.241.1436.md:98:charly.yml:79:            - '@github.com/opencharly/charly/candy/charly:v2026.240.0831'
./CHANGELOG/2026.241.1436.md:99:charly.yml:163:            - '@github.com/opencharly/charly/candy/charly:v2026.240.0831'
./CHANGELOG/2026.241.1436.md:100:charly.yml:891:            - '@github.com/opencharly/charly/candy/charly:v2026.240.0831'
./CHANGELOG/2026.241.1436.md:101:charly.yml:979:            - '@github.com/opencharly/charly/candy/charly:v2026.240.0831'
./CHANGELOG/2026.241.1436.md:102:candy/charly-marketplace/charly.yml:17:            - '@github.com/opencharly/charly/candy/charly:v2026.240.2219'
./CHANGELOG/2026.241.1436.md:103:candy/charly-marketplace/charly.yml:25:            - '@github.com/opencharly/charly/candy/charly-mcp:v2026.240.2219'
./CHANGELOG/2026.241.0323.md:59:$ git show v2026.241.0311:box/charly-arch/charly.yml | grep -oE '@github.com/opencharly/charly/candy/[a-z-]+:v[0-9.]+'
./CHANGELOG/2026.241.0323.md:60:@github.com/opencharly/charly/candy/charly:v2026.240.0759
./CHANGELOG/2026.241.0323.md:61:@github.com/opencharly/charly/candy/charly-mcp:v2026.240.0759
./CHANGELOG/2026.241.0033.md:44:   candy "github.com/opencharly/charly/candy/charly-marketplace" candy: unknown candy "github.com/opencharly/charly"
./CHANGELOG/2026.241.0033.md:68:`charly/candy/charly-marketplace` (where it is a self-reference to its own repo root) and
./CHANGELOG/2026.231.1258.md:45:Every `@github.com/opencharly/charly/candy/...` pin in this repo moves from
exit=0

$ grep -rn 'charly/candy' --include='*.yml' .
exit=1

$ grep -rn 'single tag' .
exit=1
```

The corrected pacstrap renderer path and symbol are the live source (the old
`charly/build.go` claim was the doubled-prefix mistake — the real path is the
`sdk` module):

```
$ grep -n 'func RenderPacstrapExtraConf' /home/atrawog/Sync/Atrapub/oo/opencoder/opencharly/sdk/buildkit/build_helpers.go
146:func RenderPacstrapExtraConf(p *spec.Pacstrap) string {
$ ls /home/atrawog/Sync/Atrapub/oo/opencoder/opencharly/sdk/buildkit/build_helpers.go
/home/atrawog/Sync/Atrapub/oo/opencoder/opencharly/sdk/buildkit/build_helpers.go
```

The corrected DAG leaf is the live Arch base pin (pulled from the
`distro-arch` submodule):

```
$ grep -rn 'base: quay.io/archlinux/archlinux' /tmp/opencode/distro-arch-refs/box/arch/charly.yml
12:        base: quay.io/archlinux/archlinux:base-20260719.0.558306
```

The schema hard-fail quote is the live source string (real source path is
`opencharly/sdk`, not the speculative `spec/loaderkit`):

```
$ grep -rn 'newer than this charly supports' /home/atrawog/Sync/Atrapub/oo/opencoder/opencharly/sdk/loaderkit/load_unified.go
83:			"%s: config schema %s is newer than this charly supports (max %s). Update charly (reinstall the latest opencharly package, or run 'task build:binary' from a fresh checkout and use ./bin/charly)",
```

## Rulebook compliance

- **R1** — every divergence was root-caused (candy de-submodule cutover; renderer
  move into the `sdk` module) before rewriting; no "pre-existing" classification.
- **R2** — the whole cutover is finished: the claim-keyed sweep above covers every
  in-scope surface (README prose + all `*.yml` candy refs + `single tag`), with
  only CHANGELOG history remaining.
- **R3 — no duplication** — documentation-only; the corrected ref shape and each
  corrected path are stated once, not duplicated across files.
- **R4** — N/A — documentation-only; no sleeps, retries, suppressions, or
  fallback branches are introduced.
- **R4a — binary-only commands** — every command the README tells a reader to run
  (`charly box build`, `charly -C box/cachyos box build`, `charly --repo
  opencharly/distro-cachyos box build`, `charly -C box/cachyos update`, `charly
  --repo opencharly/distro-cachyos update`) works with only the `charly` binary;
  no clone, `cd`, or `task` target is required.
- **R5** — the stale ref shape, the wrong DAG leaf, and the dead `charly/build.go`
  path are removed completely (not shimmed); the sweep above is the self-test.
- **R6** — edits were made in the fresh `/tmp/opencode/distro-cachyos-refs`
  worktree of the submodule (never in the umbrella checkout); `git status` was
  clean before editing; landing is PR-only, no force-push, no direct push to
  `main`.
- **R7** — N/A — documentation-only; no runtime behavior changed, so no test can
  fail without the change.
- **R8** — N/A — documentation-only; no labels, plans, configs, schemas, or
  generated files are emitted or changed.
- **R9** — N/A — documentation-only; no binary is built or invoked here.
- **R10** — N/A — documentation-only; no image is generated or deployed and no
  bed is invented (R10's docs clause).

## Change classification

documentation-only → `documentation reviewed` (the whole diff is markdown; no pins
or scripts changed). Materially the cachyos README only.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*