# 2026.228.0000 — sweep the check beds to the nFPM package name + build path

The nFPM packaging cutover (main repo Phase 3) retires the `opencharly-git`
package name and the `pkg/arch` makepkg source-build: the native packages are
now built by the `charly generate-packages` plugin (sdk/packagekit) from the
charly candy's `packaging:` section, and the package is named `charly`. This
change sweeps the two check beds that still asserted the old name and mechanism:

- `check-cachyos-vm` / `check-cachyos-gpu-vm`: the `pacman -Q opencharly-git`
  proper-package assertions become `pacman -Q charly`, and the bed descriptions
  that described the localpkg source-build ("builds opencharly-git from the
  superproject pkg/arch via makepkg") now describe the plugin-based build from
  the `packaging:` section.

Placeholder CalVer — the pr-validator finalizes it at merge.
