# 2026.250.1116 — feat: crabbox v0.51.0 — generated noHostname config, beds off the env knob, pins bumped

## Summary

Closes the crabbox no-hostname wave on the CachyOS stack — the released **CLI v0.51.0** (upstream #1924) with the opt-in driven by the **charly-generated config file** instead of the pod env:

- **crabbox-nesting-dns** renders `/home/user/.config/crabbox/config.yaml` with `localContainer.noHostname: true` via the new charly `config:` plan-step verb (`validate: crabbox-yaml` — the egress schema); the nesting recipe cannot set a lease hostname (crun sethostname EPERM at uid-1000 under userns=host).
- **check-crabbox-e2e-pod**: `CRABBOX_LOCAL_CONTAINER_NO_HOSTNAME` env knob DELETED; new `cbx-config-file` + `cbx-config-show` checks assert the generated config in-venue.
- Version assertions at `0.51.0`; pins layer-crabbox v2026.250.1023 / plugin-crabbox + pod-crabbox v2026.250.1019; plan/crabbox-loop-cutover.md exit-2 LANDED.

## How tested — TERMINAL R10 (final tree, fresh rebuild, zero crab overrides)

charly binary = merged charly main (v2026.250.1046, the config: verb + wave pins live); distro project = THIS branch; no `CHARLY_REPO_OVERRIDE` for any crab repo (all pins resolve merged tags; only the umbrella-root local reference — the sandbox default — is noted by the runner).

```
EVAL BED: check-crabbox-pod — exit 0 (13/13 steps)
  PASS [crabbox-verb-dispatches]  0.51.0
  PASS [cbx-cli-binary]           ok
  PASS [cbx-verb-version]         0.51.0
  PASS [cbx-verb-providers]       agent-sandbox
  PASS [cbx-verb-config]          config=/home/user/.config/crabbox/config.yaml
  PASS [cbx-verb-doctor]          ok

EVAL BED: check-crabbox-e2e-pod — exit 0 (13/13 steps)
  PASS [crabbox-verb-dispatches]  0.51.0
  PASS [cbx-e2e-prepull]          exit=0
  PASS [cbx-e2e-health]           status=200
  PASS [cbx-e2e-ready]            status=200
  PASS [cbx-e2e-login]            exit=0
  PASS [cbx-e2e-doctor]           ok  config=/home/user/.config/crabbox/config.yaml permissions=0600
  PASS [cbx-e2e-usage]            usage month=2026-09 user=crabbox@local
  PASS [cbx-e2e-leases]           exit=0
  PASS [cbx-config-file]          ok        <- generated config EXISTS (the config: verb rendered it)
  PASS [cbx-config-show]          exit=0    <- crabbox config show reads provider from the generated file (env knob GONE)
  PASS [cbx-e2e-loop]             exit=0    <- THE FULL LOCAL LOOP, hostname-less via the generated noHostname opt-in
```

Both beds: full step matrix 13/13, exit 0, update leg green, host clean. Logs: .check/check-crabbox-pod/2026.250.1056 + .check/check-crabbox-e2e-pod/2026.250.1100 (summary.yml + check-live.log).

## Rulebook compliance
- R0: layer/check/cachyos skills loaded.
- R1: the config-FILE name verified live against the v0.51.0 binary (config.yaml, NOT crabbox.yaml); a YAML plain-scalar defect in the cbx-config-file description was caught by the pre-flight gate, RCA'd (colon-space in a description) and fixed in this branch before the run.
- R5: the env knob + the gate-era fork-override narrative delete in the same PR.
- R7/R10: terminal fresh-rebuild disposable runs pasted above — both beds 13/13 exit 0, zero crab overrides.
- R2: one advancing batch — merged members (spec#117-120, sdk#237, plugin-egress#3, plugin-box#11, plugin-crabbox#2, pod-crabbox#3, layer-crabbox#2, plugin-deploy-pod#11, charly#558) + this PR; the umbrella gitlink sync is the batch's next step (all tags merged).
- R3/R4/R6: config generation lives once (nesting-dns via the config: verb); no workarounds; branch-per-change PR.

## Change classification

- **Class:** distro beds + candy infra (wave consumer). **Verification gate:** terminal R10 (both beds 13/13, exit 0) pasted above. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*