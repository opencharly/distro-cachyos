# 2026.251.0814 — fix(pin): box/android-emulator — layer-android-sdk v2026.250.1813 + pod-android-emulator-layer v2026.251.0751

## Summary

Stale-pin fix for the android-emulator box (`box/android-emulator/charly.yml`): the
candy list baked two PRE-FIX tags of the android wave:

- `layer-android-sdk:v2026.239.1604` → **`v2026.250.1813`** (the aapt2 fix: the
  old pin baked the `build-tools 36/` check; 1813 ships the fixed `36.0.0` check)
- `pod-android-emulator-layer:v2026.243.1812` → **`v2026.251.0751`** (the pod-layer
  PR #7 merge — its own `require:` now pins the fixed sdk 1813; the 243.1812 pin
  transitively baked the old sdk pin + old behavior)

`pod-appium-server:v2026.239.1604` stays — it IS that repo's latest tag (no fix has
shipped past it), and `layer-nodejs`/`layer-java-openjdk` are not android-wave
components.

## How tested

**Tag existence (both pinned refs resolve):**

```
gh api repos/opencharly/layer-android-sdk/git/refs/tags/v2026.250.1813 => 1813 -> e610bf31
   (contains the aapt2 fix; layer-android-sdk main contains it — compare e610bf31...main: ahead, 0 behind)
gh api repos/opencharly/pod-android-emulator-layer/git/refs/tags/v2026.251.0751 => 0751 -> 4d5bf7ae
   (= the PR #7 merge commit; pod-layer main contains it — compare 4d5bf7ae...main: ahead, 0 behind)
```

**The layers' fixed checks at the pinned tags:**

```
layer-android-sdk @ v2026.250.1813:
  check: build-tools 36 provides aapt2 (Appium's ADB badging probe needs it)
    file: /opt/android-sdk/build-tools/36.0.0/aapt2     # <- the fixed 36.0.0 path
pod-android-emulator-layer @ v2026.251.0751 (main, post-#7):
  require: - '@github.com/opencharly/layer-android-sdk:v2026.250.1813'  # aapt2 at build-tools/36.0.0
```

**Local gate:** `box/android-emulator/charly.yml` re-parses clean (yaml.safe_load OK)
and the repo's CI (charly/pr-validator) validates the box at the repo's pinned charly.

## Rulebook compliance

- **R1**: both stale pins are root-caused to the sdk aapt2 fix (v2026.250.1813) and its
  pod-layer consumer merge (#7, v2026.251.0751) — nothing left unexplained.
- **R3**: no duplication — the pins stay the box's declared layer set, just the fixed tags.
- **R4**: no workarounds — released-tag pins, no hand-pins.
- **R6**: git safety — feat/ branch only, no force-push, no direct main push.
- **R10**: merged-ref pins with evidence above; the E-5 wave bed (check-android-
  emulator-pod, disposable: true) is this box's runtime witness.

## Change classification

- **Change class**: fix (consumer pin sync — android-wave pre-fix tags)
- **Verification gate**: tag-existence + fixed-check grep at each tag + YAML gate (above)
- **Attribution tier**: analysed on a live system (the E-5 wave bed ran live; the full
  4-phase gate incl. the appium instrument finalize rides the consumer E-5 PR)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (95% confidence)*