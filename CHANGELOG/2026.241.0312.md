# 2026.241.0312 — fix(tag-on-merge): install the current org-wide dispatcher - this repo cannot tag

## Summary

**This repo cannot produce release tags.** Every `tag-on-merge` run ends in `startup_failure`:

```
$ gh run list --workflow=tag-on-merge.yml --limit 4
completed  startup_failure  tag-on-merge  main  workflow_run  33223876513  0s  2026-08-29T00:33:18Z
completed  startup_failure  tag-on-merge  main  workflow_run  33222002364  1s  2026-08-28T23:56:15Z
completed  startup_failure  tag-on-merge  main  workflow_run  33217676398  1s  2026-08-28T22:40:08Z
completed  startup_failure  tag-on-merge  main  schedule      33217632339  1s  2026-08-28T22:39:24Z
```

The first line is PR #29's merge, at its exact merge time. `main` is **4 commits ahead of the
newest tag** `v2026.238.1353`:

```
$ git log --oneline v2026.238.1353..origin/main
dbddcaa fix: reference charly's candies individually - the repo root is not a candy (#29)
a57423a fix: pin the merged pod-selkies-desktop — the selkies chain now resolves (#28)
2e3fb4d feat: distro sweep — advance all @github candy pins to latest tags … (#27)
f9810ab fix: advance layer-supervisord refs to v2026.239.1300 … (#26)
```

Two of those (#28, #29) are the repairs to the selkies chain pins. They are merged and invisible
to any consumer pinning this repo by tag.

## Root cause

The org reusable workflow this dispatcher calls declares job-level permissions:

```yaml
# opencharly/.github/.github/workflows/tag-on-merge.yml
jobs:
  tag-and-changelog:
    permissions:
      contents: write
      pull-requests: read
```

A caller that sets an explicit `permissions:` block makes **every scope it does not list `none`**,
and a reusable workflow may not request more than its caller's ceiling — so the call is rejected
before any job starts. That is precisely `startup_failure` with no job to inspect. This repo's
dispatcher granted only:

```yaml
permissions:
  contents: write        # pull-requests is therefore `none`
```

Repos that tag successfully today (`marketplace`, `charly`) grant `pull-requests: read`.

The dispatcher was also **two revisions behind**: still on the `schedule` cron trigger the org
retired after measuring 21–44 minute delivery gaps with no SLA, rather than the event-driven
`workflow_run` + `push` form with the bounded `wait-for-merge` readiness probe.

## How tested

Installed **verbatim** — byte-identical to the copy running in repos that tag successfully:

```
$ diff .github/workflows/tag-on-merge.yml <marketplace>/.github/workflows/tag-on-merge.yml
$ echo $?
0
```

No local variation, deliberately: a forked dispatcher is how this drifted in the first place. The
diff against this repo's previous file is `+  pull-requests: read` plus the trigger and
readiness-probe revision.

**The proof is this PR's own merge.** If a CalVer tag appears on the merge commit, the dispatcher
works — where the previous four merges produced none. That is directly checkable, and I will
confirm it on the merge commit rather than assert it here.

## Rulebook compliance

- **R1** — root-caused before remediating: `startup_failure` with no job is explained by the
  caller-ceiling rule and corroborated by the working repos granting the missing scope.
- **R3 / harness parity** — restored to the single org-wide source rather than locally patched; a
  minimal `+ pull-requests: read` would have left this repo two revisions behind on a retired
  trigger.
- **R4** — no workaround: no tag is hand-created and the cron sweep is not re-tuned.
- **Rule 1** — this repo's own workflow file only; no submodule touched.

## Related

`distro-arch#29` is the same fix for the same defect in that repo, and it is the one that matters
for this repo's validation: `distro-arch` cannot tag either, so this repo still pins
`distro-arch:v2026.236.0159`, whose refs reach a **pre-cutover charly** that still carries
`candy/plugin-mcp` and `candy/plugin-record`. Measured here, overriding only that repo:

```
$ charly box validate                                    # distro-arch at the pinned tag
conflicts: 4      total errors: 7
$ CHARLY_REPO_OVERRIDE=github.com/opencharly/distro-arch=<distro-arch@main> charly box validate
conflicts: 0      total errors: 3
```

Six further repos carry the same broken dispatcher (`distro-debian`, `distro-fedora`,
`distro-ubuntu`, `pkg-arch`, `pkg-debian`, `pkg-fedora`), each fixed in its own PR. The canonical
template in `opencharly/.github` is **already the current form** — these repos drifted by never
being re-synced from it, not by copying a stale template.

## Change classification

- **Class:** one CI dispatcher restored to the org-wide version.
- **Verification gate:** byte-identical diff against a repo where the dispatcher demonstrably
  tags; the end-to-end gate is this PR's own merge producing a tag.
- **Attribution tier:** `analysed on a live system` — the failure was observed live in this repo's
  run history, tied to a specific merge, and the downstream cost measured live with a repo
  override and retained output. The dispatcher cannot be exercised before merge, so I claim no
  more.

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*