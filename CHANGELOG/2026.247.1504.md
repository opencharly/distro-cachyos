# 2026.247.1504 — feat: the FULL LOCAL LOOP lands in check-crabbox-e2e-pod (loop-cutover exit 2)

## Summary

The **loop-cutover exit-2** landing (plan/crabbox-loop-cutover.md):

- **check-crabbox-e2e-pod**: restored `cbx-e2e-loop` (the FULL LOCAL LOOP:
  `crabbox run --provider local-container` on the nested rootless podman with
  the `crabbox-local-ok` stdout assertion) + the `cbx-e2e-prepull` step.
  The loop is **gated on the upstream knob**: the bed env carries
  `CRABBOX_LOCAL_CONTAINER_NO_HOSTNAME=1`, which requires openclaw/crabbox
  #1813 (the no-hostname knob) in the released CLI — documented, not hidden.
- **Host-backed storage volume** (`cbx-storage`): the nested podman user
  graphroot moves to a volume-backed upper (single-fuse), removing the
  nested fuse-overlayfs dir-create EPERM that blocked the loop (the
  terminal-blocker matrix in the plan).
- **Priority-1 prewarm service** (`cbx-storage-prewarm`): pulls the smoke
  alpine at container-up, so the baked `--pull=never` nesting smoke passes
  against the empty volume store.
- **/tmp-1777 + cache-ownership build fixes** (RCA 2026.247.1406/1416):
  supervisord boot + the lease bootstrap at uid-1000.
- The local `crabbox-nesting-dns` candy owns the volume + the storage.conf
  override (R3: one home for the nesting glue).

## How tested — the FULL LOCAL LOOP proven in-box

```
$ CHARLY_REPO_OVERRIDE=opencharly/layer-crabbox=<local knob override>,... ch
arly check run check-crabbox-e2e-pod   # calver 2026.247.1452
[image-build] PASS 139s [check-image] PASS [deploy-add] PASS [config] PASS
[start] PASS [bring-up-members] PASS [check-live] PASS (106/0 plan checks)
[feature-run] PASS [update] PASS (R10 fresh-rebuild gate)
[check-live-rebuild] PASS [feature-run-rebuild] PASS [cleanup] PASS
check-crabbox-e2e-pod: PASS (steps=13) — EXIT 0
cbx-e2e-loop  PASS exit=0 (the FULL LOCAL LOOP: lease/sync/run/stream/release)
nested-podman-run PASS exit=0 (--pull=never alpine, prewarmed)
cbx-e2e-health/ready 200 · login/doctor/usage/leases PASS
crabbox-verb-dispatches -> 0.49.0-knob2 (the knob CLI, live)
host clean after (containers/units/volumes removed)
```

The knob CLI (0.49.0-knob2) was provided via the sanctioned
`CHARLY_REPO_OVERRIDE` mechanism: a local layer-crabbox override pointing at a
knob release on the org fork (opencharly/crabbox, the fork of openclaw/crabbox
behind upstream PR #1813). The loop + this run are the ledger proof; the
same bed run on the RELEASED CLI (0.48.1) is red at the loop step until #1813
releases — the gate is documented in the plan + this body.

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | check/nesting/layer skills loaded before authoring. |
| R1 — RCA every anomaly | Every round RCA'd + root-fixed (hostname class -> upstream knob; storage fuse EPERM -> volume-backed upper; smoke ordering -> prewarm; /tmp + cache ownership); 0 errors; warnings = inherited multi-version pins + dev-worktree notice (allowlisted/inherited). |
| R2 — finish the whole cutover | The loop landing + its infrastructure ship in ONE PR with the gate documented. |
| R3 — no duplication | Storage/prewarm glue lives once in the local candy; upstream knob referenced, not re-implemented. |
| R4 — no workarounds | The fork-build proof uses the sanctioned override mechanism; no sleeps/suppressions. |
| R5 — delete legacy | N/A. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | The FULL loop executed live (steps=13 incl. the R10 update gate), evidence pasted. |
| R8 — preserve emitted artifacts | `box validate` 0 errors; supervisord init asserted. |
| R9 — binary equals source | Fresh worktree-built bin/charly. |
| R10 — fresh disposable proof | Full fresh-rebuild R10 on the `disposable: true` bed, matrices above, host clean. |
| RDD/ADE/SDD | Descriptions/plans updated with the change; no spec changes (N/A SDD). |
| Concurrency | Sequential bed runs only. |
| Disposable-only gate | Only the `disposable: true` e2e bed mutated. |
| Hooks doctrine | N/A; merge gate = org pr-validator. |

## Change classification

- **Class:** distro bed + candy infrastructure (loop landing, gated).
- **Verification gate:** `charly box validate` (0 errors) + the R10 bed proof
  (steps=13, loop PASS, fork-build knob documented).
- **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*