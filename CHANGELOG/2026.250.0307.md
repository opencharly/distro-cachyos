# 2026.250.0307 — chore(schema): unroll the group: node + the converted-arch import bump (Cutover C task 1)

## Summary

Cutover C task 1 for **opencharly/distro-cachyos** (named batch: `cutover-c`): the last member-bearing `group:` node `check-punktfunk-fleet` is unrolled onto the post-#105 spec (v0.2026250.158) — `group: kind` is REMOVED from `#ResourceKind` (pod/vm/kubernetes/local/android remain) and `Deploy.IsGroup()` is deleted.

Three semantic changes, each accounted:

1. **The unroll:** `check-punktfunk-fleet` (the fleet bed: streaming host + client pairing) — the **host member becomes the primary `pod:` substrate** and keeps its scalars (image, volume, plan); **`punktfunk-fleet-2-client` stays a deploy-level sibling**. The `1-`/`2-` sorted-key ordering contract, the PIN bind-mount channel, and every check step are unchanged — only the grouping wrapper is gone. `git grep "group:"` across the tree: **zero matches**.
2. **Peer rename:** the `cachyos-gpu` deploy's bare streaming peer `selkies-kde` → `selkies-kde-gpu-stream`. The new spec validator enforces **global peer-name uniqueness**; the `check-cachyos-gpu-vm` bed already owns the peer name `selkies-kde` (its in-guest quadlet checks reference `charly-selkies-kde`, so THAT peer keeps the name). A pre-existing latent collision, first surfacing once validation proceeds past the arch import.
3. **Import pin bump:** `arch` import `v2026.243.1842` → **`v2026.250.0229`** — the first distro-arch release carrying the converted tree (#34 merged); the previous pin is the pre-conversion schema and fails project resolve under the new spec.

**Accounting for the pervasive re-wrap (the rest of the diff):** the remaining hunks (`check-punktfunk-pod`, `check-punktfunk-vm`, `check-punktfunk-client`, `check-cardwire-vm`, `check-crabbox-pod`, `check-crabbox-e2e-pod`, `check-cstream-{gpu-,}vm`, `check-cstream-pod`, …) are the **required re-render side effect of the migrate unroll** under the post-#105 schema emission: multi-line `description: >-` blocks re-wrapped to single lines and blank-line normalization. No semantic delta — every re-wrapped hunk is content-identical modulo line wrapping (verified against the diff), and the whole-tree gate below proves parse+validate equivalence.

## How tested

Binary: `/tmp/charly-cc1` — charly built from `feat/drop-group-kind`, `go version -m` confirms **sdk v0.2026250.154** (includes the fleet-del ledger-lock fix, sdk #227) and **spec v0.2026250.158**.

1. `charly box validate` — **EXIT 0** on the final committed tree (`d73ce78`):
   ```
   charly box validate: OK — checked 505 candies, 40 boxes, 41 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
   ```
   (Gate log: `/tmp/cc1-cachyos-boxvalidate.log`. Before the arch pin bump this failed at import resolve — `node "check-agent-live": no kind discriminator`, the pre-conversion distro-arch tree; fixed by the `v2026.250.0229` bump, after which the latent peer-name collision surfaced and was fixed in-tree.)
2. `charly check run check-punktfunk-fleet` (timeout 1800s) — **BLOCKED UPSTREAM; routed to the named batch `cutover-c`, tracked in #71** (not a bare follow-up):
   ```
   [image-build-punktfunk-fleet-2-client] FAIL after 1m41.268s: exit 1
   error: target not found: punktfunk-client
   ```
   **RCA:** the upstream punktfunk pacman repository `https://git.unom.io/api/packages/unom/arch/punktfunk` (punktfunk's OWN third-party repo) indexes **only** `punktfunk-gamescope-3.16.25.pfhdr8-1` — `punktfunk-client` AND `punktfunk-host` are absent from the repo DB (fresh cache-busted fetches, twice, 2 min apart), while the `0.35.0-0.00020538` packages DO exist in the Gitea registry package list — an upstream index lag/regression. The pacman bake lives in the external `@github.com/opencharly/layer-punktfunk-client@v2026.243.1116` ref — **not fixable in distro-cachyos**. Retained run: `.check/check-punktfunk-fleet/2026.250.0256/`.
   **Named-batch routing:** #71 carries the full RCA, the exact exit condition (full R10 matrix on the merged head once upstream republishes) and the re-run command. **Exit advanced in THIS PR:** the cutover task itself — this repo's last `group:` node is unrolled (`git grep "group:"` = 0), the converted-import resolve is restored, and the tree is validate-green, so the bed is runnable the moment upstream republishes; no further distro-cachyos change is required or expected.
   **Pattern proof:** the unroll applied here is the SAME migrate unroll pattern proven live by the 5 fedora distro beds (all PASS post-identity-updates in the same wave) — the conversion pattern itself is runtime-proven; this bed's blocker is upstream only.
3. Working tree clean; branch current with `origin/main` (0 behind).

## Rulebook compliance

- **R0 (skills first):** git-workflow + strict-policy + root-cause-analyzer loaded and applied; the bed failure got full RCA (upstream repo DB forensics) before any escalation — no blind retries.
- **R1 (RCA):** every anomaly RCA'd — the arch-import resolve failure (pre-conversion tag), the peer-name collision (latent, surfaced by the new validator), and the bed image-build failure (upstream package-repo index).
- **R2 (no split):** no forbidden follow-up framing — the bed R10 residue is routed to the NAMED batch `cutover-c` with a tracking issue (#71) that names the exit condition and the re-run command; the exit this PR advances is stated in How-tested.
- **R3 (no duplication):** no behavior re-implemented; the unroll preserves the bed's semantics verbatim; the shared migrate unroll pattern is reused, not forked.
- **R4 (no workarounds):** no sleeps, no cache hand-edits, no hand-pinned unmerged refs, no vendored-package workaround for the upstream regression; the arch bump targets a MERGED, TAGGED release.
- **R5 (delete legacy):** the `group:` node is fully removed from this tree — `git grep "group:"` = 0 matches; no shim, no compatibility alias.
- **R6 (git safety):** append-only commits on `feat/drop-group-kind` (`37f04bf` → `db892c1` → `d73ce78`); no force-push, no main push, no hook bypass.
- **R7 (prove the gate):** `charly box validate` pasted above, EXIT 0 on the final committed tree.
- **R8 (pin discipline):** only merged refs pinned — `distro-arch@v2026.250.0229` is a published release tag; `@github` candy refs resolve to latest tags at runtime (`plugin-deploy-pod v2026.249.2212`, `plugin-vm v2026.249.1732` observed).
- **R9 (policy B):** this IS the distro conversion — the tree now conforms to charly's post-#105 gitlinks/spec contract; no hand-pin divergence introduced.
- **R10 (fresh disposable proof):** validate run from the final committed tree; the changed bed's fresh-disposable proof is upstream-blocked and routed to #71 with the retained run evidence.

## Change classification

- **Change class:** schema migration (CalVer cutover) + test-bed unroll + dependency pin bump; single-file (`charly.yml`, +248/−338, of which the re-render re-wrap is content-identical).
- **Verification gate:** `charly box validate` (EXIT 0, pasted) + the converted-import resolve; the changed bed's R10 is upstream-blocked, routed to the named `cutover-c` batch via #71.
- **Attribution tier:** `analysed on a live system` — box validate and the conversion verified live on the committed tree; the bed R10 is blocked by the upstream punktfunk package-repo regression documented above.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*