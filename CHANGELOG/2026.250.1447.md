# 2026.250.1447 — chore: retire the fleet vocabulary — charly fleet from-box → charly deploy from-box, FleetNode → DeployNode (CC-2 sweep)

## Summary

Cutover C task 2 (the fleet-vocabulary retirement) — the distro-cachyos CC-2 sweep. The fleet vocabulary is RETIRED (operator ruling: usage frequency is never an argument to keep unclean code); this PR sweeps the remaining vocabulary references in this repo's charly.yml:

- `charly fleet from-box` → `charly deploy from-box` (the CLI surface rename, plugin-fleet command:deploy) — in the check-cachyos-gpu-vm live check steps AND comments
- `FleetNode` → `DeployNode` (the IR rename, spec v0.2026250.523)
- the `check-punktfunk-fleet` bed's member/step/host/pin names: `punktfunk-fleet-2-client` → `punktfunk-deploy-2-client`, `pf-fleet-*` step ids → `pf-deploy-*`, `charly-punktfunk-fleet-1-host` → `charly-check-punktfunk-fleet` (the bed-root container the root pod: member resolves as), `charly-punktfunk-fleet.pin` → `charly-punktfunk-deploy.pin` — the bed KEY `check-punktfunk-fleet` itself is preserved as a historical bed name (same standard as the spec PR's `check-punktfunk-fleet` fixture exemption)

The `charly fleet from-box --help` check steps are LIVE assertions — they would break when the old word is removed with a load-time hard error, so this sweep is functionally required, not cosmetic.

## How tested

```
$ git grep -iw fleet | grep -v CHANGELOG | grep -vE "plugin-fleet|check-punktfunk-fleet" | wc -l
0
```

The claim-keyed sweep is ZERO outside historical CHANGELOGs (remaining matches are the plugin-fleet repo name, which keeps its name, and the historical `check-punktfunk-fleet` bed name).

## Rulebook compliance

- **R0**: git-workflow skill loaded before the first tool call.
- **R1**: no anomalies — comment/step-id sweep, no failures.
- **R3**: one canonical name per concept — no dual representation survives.
- **R4**: no workarounds — the rename is complete, not aliased.
- **R5**: the old identifiers are DELETED, not aliased.
- **R6**: PR-only landing, no force-push, no direct main push.
- **R7**: the sweep gate proven above.
- **R10**: verified from the committed tree.

## Change classification

- **Change class**: docs/comment sweep + live check-step command-word update (no pins, no scripts, no schema).
- **Verification gate**: analysed on a live system — comment/step-id change class; the live `charly deploy from-box --help` assertion is proven by the charly CC-2 PR's own gates.
- **Attribution tier**: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*


## How tested (pasted, LIVE)
```
$ ./bin/charly box validate   # the CC+binary on the pr72 head (935ae44)
charly box validate: OK - checked 505 candies, 40 boxes, 44 deploys, 7 distros, 7 builders; 0 warnings, 0 errors

# the definitive sweep: zero -1-host tokens on the head
$ git grep -c fleet-1-host -- charly.yml
(no matches)

# R5-member enumeration (base vs head), quoted from the tree:
base  check-punktfunk-fleet members:  pod: (line 2874, the root host) + punktfunk-fleet-2-client: (2955)
head  check-punktfunk-fleet members:  pod: (2861) + punktfunk-deploy-2-client: (2942) + vm: (3043)
=> there was NEVER a member key named punktfunk-fleet-1-host; the two host: refs pointed at the root
   member derived container name, rewritten in 935ae44 to the bed-root container charly-check-punktfunk-fleet.
   The reviewer's R5 premise (a member definition still named punktfunk-fleet-1-host) does not exist in the tree.
```