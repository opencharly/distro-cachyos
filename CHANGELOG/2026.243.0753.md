# 2026.243.0753 — chore: complete the pin advance charly#506 started

## Summary
    layer-docs-site             v2026.239.1618 -> v2026.243.0408
    pod-dsh                     v2026.239.1643 -> v2026.243.0410
    pod-nested-podman-socket    v2026.239.1601 -> v2026.243.0411

charly#506 advanced 11 stale candy pins in charly's own charly.yml. This repo's
candy/charly-marketplace pins three of the same candies and was not advanced with it, so charly
composes each of them at TWO versions and warns, six times in one validate:

    Warning: candy github.com/opencharly/pod-nested-podman-socket resolved to multiple versions;
    using newest 2026.243.0100 from v2026.243.0411, ignoring 2026.229.1100 from v2026.239.1601

Same for layer-docs-site and pod-dsh. The three new versions here are exactly the ones charly
pins, so after this the closure resolves one version per candy.

This is the second time in this session that a repin landing one link deep left the links above
it stranded - a partial repin looks like progress while changing nothing about the split it was
meant to close. The rule that keeps falling out: advance every path to the candy, not the first
one found.

## How tested

    charly box validate
    exit 0

    charly box validate, counting multi-version warnings
    0

Before this commit that count was 6 when charly composed this repo as box/cachyos - 3 candies
across 2 resolution passes. Measured from charly's root, which is where the composition, and
therefore the split, actually happens.

## Rulebook compliance

- **R1 — RCA every anomaly.** Six multi-version warnings appeared the moment charly's `main` was
  merged into a working branch. They were traced to their pinner (`grep` over charly's own fetch
  cache → `distro-cachyos@.../candy/charly-marketplace/charly.yml`) rather than allowlisted or
  attributed vaguely to "org skew".
- **R2 — Finish the whole cutover.** Not parked as "pre-existing" despite arriving from
  charly#506: the warnings surfaced in an active cutover, so they are fixed here. charly#506 is
  the named batch this completes.
- **R3 — No duplication.** The pins are advanced to exactly the versions charly already pins; no
  new source of truth.
- **R4 — No workarounds.** No allowlist entry; the split is closed at its cause.
- **R6 — Git safety.** Branched from a fresh `origin/main`; no force-push, no hook bypass, no
  direct push to main.
- **R7 — Prove behavior, not compilation.** `charly box validate` pasted, plus the warning count
  measured from charly's root — the venue where the composition, and therefore the split,
  actually occurs. A count taken from this repo's own root would have read 0 before the fix and
  proved nothing.

## Change classification

| | |
|---|---|
| **Change class** | pin advance (three `@github` refs in one candy manifest) |
| **Verification gate** | `charly box validate` here, and the composed multi-version warning count from charly's root going 6 → 0 |
| **Attribution tier** | `analysed on a live system` — validate passed and the composed warning count was measured live before and after; no bed in this repo exercises these three candies |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (analysed on a live system)*