# 2026.246.1545 — feat(beds): check-vm-clone — the VM-on-VM layering R10 beds

## Summary

Add the VM-on-VM layering R10 beds (cutover task 9), modeled on check-cachyos-vm. The base bed provisions the cachyos VM, runs in-guest command probes, captures the `golden` snapshot at install finalize, and keeps the venue so the disk survives teardown. The clone bed deploys a `source.kind: clone` entity (from the base bed's golden) and probes the cloned guest — proving the clone build path and the layered bake chain on a disposable cloud-init VM.

## How tested

`charly check run` on both beds with the in-development stamped binary:

**check-vm-clone-base — PASS (steps=8):** vm-build 1m47s / vm-create 2.4s / deploy-add 27s / check-live 5.5s (in-guest probes: uname -n, os-release ID=arch) / **update 31.5s (fresh-rebuild gate)** / check-live-rebuild / **snapshot-capture (golden)** / cleanup.

**check-vm-clone — PASS (steps=9):** vm-build 1.5s (clone disk from golden) / vm-create (per-domain seed attached) / **deploy-add 26s (cloned guest reachable over SSH)** / check-live 5.4s / **update 31s (fresh-rebuild gate)** / check-live-rebuild / cleanup.

Probe-reality findings encoded in the beds: the guest lacks the `hostname` binary (`uname -n`), and the cachyos bootstrap base reports `ID=arch` (distro-repo note: CachyOS packages, unrelabeled os-release).

## Rulebook compliance

| Rule | HOW / N/A |
|---|---|
| R0 (skills honored) | The check/disposable skills read before authoring; beds modeled on check-cachyos-vm. |
| R1 (RCA every anomaly) | Every bed failure was root-caused and fixed: hostname-absent → uname -n; ID=arch reality; agent requirement for consistent capture → plain external; keep_venue needed (teardown deletes golden); clone per-domain seed gap (plugin-vm fixes). |
| R2 (no split) | N/A — one bed cutover. |
| R3 (one implementation) | Beds use the existing check-run machinery + snapshot policy; no custom orchestration. |
| R4 (no workarounds) | N/A — timeouts/waits are the check framework's own eventually/retry mechanics, not hand-rolled. |
| R5 (delete legacy) | N/A — additive. |
| R6 (git safety) | feat/ branch, no force-push. |
| R7 (prove the gate) | Both beds PASSED via `charly check run`, output above. |
| R8 | N/A — candy YAML, no binary. |
| R9 | N/A — no new deps. |
| R10 (fresh disposable proof) | Both beds PASSED all steps on disposable: true cachyos targets with the in-development binary, including the fresh-update rebuild gates (pasted above). |

## Change classification
- Change class: feature (R10 test beds)
- Verification gate: `charly check run` on both beds (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*