# 2026.251.0836 — fix(pin): box/android-emulator — pod-appium-server v2026.239.1604 → v2026.251.0815 (transitive aapt2 fix)

## Summary

Pin bump in `box/android-emulator/charly.yml` (candy list):
`@github.com/opencharly/pod-appium-server:v2026.239.1604` → **`v2026.251.0815`**.

The 239.1604 tag is a pre-fix tag of the android wave: its own `require:` pins
`layer-android-sdk:v2026.237.805` (the aapt2 `36/`-era). v2026.251.0815 is the pod's
post-fix tag (pod-appium-server PR #3, merged) whose require now pins the fixed
`layer-android-sdk:v2026.250.1813` — so every venue composing this pod gets the
fixed SDK closure even without a direct sdk pin, and the box no longer carries any
239.x-era android reference.

## How tested

**Tag existence (the pinned ref resolves):**

```
gh api repos/opencharly/pod-appium-server/git/refs/tags/v2026.251.0815 => 9b64c7e0
  (= the merged PR #3 head; the tag-on-merge mint)
pod-appium-server @ v2026.251.0815 require:
  - '@github.com/opencharly/layer-android-sdk:v2026.250.1813'   # the aapt2 36.0.0 fix
pod-appium-server @ v2026.239.1604 require (the replaced pin):
  - '@github.com/opencharly/layer-android-sdk:v2026.237.805'   # pre-fix
```

**Fresh-rebuild of the CHANGED box composition (check-android-emulator-pod run 2026.251.0824, built from this PR's tree):**

```
image-build:  PASS (image tagged check-android-emulator-pod-2026.251.0824)
check-image:  317 steps: 183 passed, 0 failed, 134 skipped
  PASS check build-tools 36 provides aapt2 [layer-android-sdk:3]  (the FIXED 36.0.0 check)
```

**The wave bed's full lane (check-android-emulator-pod run 2026.251.0554 — the E-5 F-Droid/ApiDemos committed-APK install lane, `disposable: true`):**

```
steps:  image-build(115s) ok | check-image(47s) ok | deploy-add(18s) ok | config(8s) ok
        start(4s) ok | bring-up-members(59s) ok | check-live(213s) ok | feature-run(191s) ok
        rebuild-members-down(6s) ok | update(61s) ok | re-bring-up-members(37s) ok
        check-live-rebuild(175s) ok | feature-run-rebuild(158s) ok
total_seconds: 1091

check-live.log:146          PASS  check adb=shell  [apk-fdroid-present]  package:org.fdroid.fdroid
check-live.log:148          PASS  check adb=shell  [apk-fdroid-launch]   6294
feature-run.log:144         PASS  check adb=shell  [apk-fdroid-present]  package:org.fdroid.fdroid
feature-run.log:146         PASS  check adb=shell  [apk-fdroid-launch]   6294
(check-live-rebuild + feature-run-rebuild: same two checks PASS)
```

Run verdict note: the 0554 run's overall red is the E-5 appium-screen instrument
finalize — the distro E-5 wave's own remaining item (its consumer PR rides it as
its R10; the layer-android-test-apps #2 review accepted the same RCA'd-red
framing). All plan/baked steps incl. the android install path are green.

**Local gate:** `box/android-emulator/charly.yml` re-parses clean (yaml.safe_load
OK); the repo's CI (charly/pr-validator) validates the box at the repo's pinned
charly.

## Rulebook compliance

- **R1**: the 239.1604 pod-appium pin is root-caused as a transitive member of the
  android-wave stale-tag class (its require resolves the pre-fix sdk) — fixed via the
  released post-fix tag.
- **R3**: no duplication — the pod owns its require; the box pins its released tag.
- **R4**: no workarounds — released-tag pin, no hand-pin/raw-sha.
- **R6**: git safety — feat/ branch only, no force-push, no direct main push.
- **R10**: fresh-rebuild of the changed composition (0824) + the wave lane (0554),
  both pasted above, `disposable: true` targets.

## Change classification

- **Change class**: fix (consumer pin sync — android-wave pre-fix tag, transitive)
- **Verification gate**: tag-existence + require-content grep at both tags + the two
  pasted bed runs (fresh rebuild of the changed composition)
- **Attribution tier**: analysed on a live system (the changed composition and the
  wave lane ran live; the full 4-phase E-5 gate incl. the appium instrument
  finalize rides the consumer E-5 PR)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (95% confidence)*