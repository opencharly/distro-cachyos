# 2026.236.2135 — feat: bump layer-cuda to v2026.235.2121 (the nvidia-dep rewrite tag)

## Summary

The candy de-submodule cutover, batch 1: the batch-1 moved candies (`cuda`, `dbus`, `gemini`, `golang`, `nodejs`, `supervisord`, `tmux`, `chrome`, `chrome-cdp`, `ffmpeg`, `pipewire`, `python`, `selkies`, `socat`, `sway`) move from `@github.com/opencharly/charly/candy/*` to their standalone kind-prefixed repos, plus `nvidia` → `layer-nvidia` and the `charly` candy bump.

**This head pins charly@v2026.236.2040** (the charly#402 tag: the ONE venue-agnostic pac install body + the arch key-FILE mechanism (curl + `pacman-key --add`, no keyserver) + the mkinitcpio/pacman-hook diagnostic allowances — the vm checks pass at it; charly#400's mkinitcpio allowlist also rides the tag).

**Host-requirement comments** added on the four VM check definitions (`check-cachyos-vm`, `check-cachyos-gpu-vm`, `check-selkies-labwc-nvidia-vm`, `check-selkies-kde-nvidia-vm`): the bootstrap disk build needs ≥102 GiB on the build host's `/tmp`, redirectable with `TMPDIR` + `CHARLY_VM_STATE_DIR`.

## How tested

**The diff (5 files, +35/−15):**

```
$ git diff origin/main..HEAD --stat
 box/githubrunner/charly.yml     |  2 +-
 box/nvidia/charly.yml           |  4 ++--
 box/openclaw-desktop/charly.yml |  2 +-
 box/versa/charly.yml            |  4 ++--
 charly.yml                      | 38 +++++++++++++++++++++++++++++---------
 5 files changed, 35 insertions(+), 15 deletions(-)
```

**The sweep at THIS head (no leftover old-form refs to any moved candy, including nvidia, excluding CHANGELOG):**

```
$ grep -rlE "@github.com/opencharly/charly/candy/(cuda|dbus|gemini|golang|nodejs|supervisord|tmux|chrome|chrome-cdp|ffmpeg|pipewire|python|selkies|socat|sway|nvidia):v[0-9.]+" box/ charly.yml | grep -v CHANGELOG
(no output — 0 hits)
```

**The refs this head pins (verified via `gh api` tags listing):**

```
charly: v2026.236.2040   layer-nvidia: v2026.235.2120   layer-cuda: v2026.235.2121
pod-dbus: v2026.235.2115  layer-gemini: v2026.235.2110  layer-golang: v2026.235.2057
layer-nodejs: v2026.235.2056  layer-supervisord: v2026.235.2056  layer-tmux: v2026.235.2057
layer-chrome: v2026.235.2057  pod-chrome-cdp: v2026.235.2110  layer-ffmpeg: v2026.235.2057
pod-pipewire: v2026.235.2110  layer-python: v2026.235.2057  pod-selkies: v2026.235.2110
layer-socat: v2026.235.2057  pod-sway: v2026.235.2115
```

**THIS repo's bed at THIS head** (check-cachyos-vm — the bed whose `add_candy` charly ref advanced; run from the umbrella's distro-cachyos submodule with a charly binary built at v2026.236.2040, `TMPDIR` + `CHARLY_VM_STATE_DIR` redirecting the disk build to the 1.5T filesystem):

```
$ TMPDIR=/home/atrawog/.cache/charly-tmp CHARLY_VM_STATE_DIR=/home/atrawog/.local/share/charly/vm2 charly check run check-cachyos-vm
charly check run check-cachyos-vm: [vm-build] PASS after 4m41.503s [allowlisted=6]
charly check run check-cachyos-vm: [vm-create] PASS after 2.368s
charly check run check-cachyos-vm: [deploy-add] PASS after 1m22s (errors=0 warnings=11 allowlisted=14)
charly check run check-cachyos-vm: [bring-up-members] PASS
charly check run check-cachyos-vm: [check-live] PASS
charly check run check-cachyos-vm: [update] PASS after 1m30.841s (errors=0 warnings=11 allowlisted=14)
charly check run check-cachyos-vm: [check-live-rebuild] PASS
charly check run check-cachyos-vm: [cleanup] PASS
charly check run check-cachyos-vm: PASS (steps=9)
```

Retained: `distro-cachyos/.check/check-cachyos-vm/2026.236.2046/` — every step `ok: true`; deploy-add `errors: 0` (the charly package installs from the published charly-arch repo via the curl + `pacman-key --add` key-FILE mechanism — the `installing charly...` line is in the deploy-add log; the previously-failing `target not found: charly` is GONE). All diagnostic lines allowlisted (mkinitcpio autodetect + pacman-hook + chroot warnings + cachyos zstd/pacman-needed).

**layer-nvidia@v2026.235.2120 composition at THIS head** (box/nvidia — the box whose refs this PR advances; resolves layer-nvidia@2120 + layer-cuda@2121):

```
$ charly box build nvidia
COMMIT ghcr.io/opencharly/arch:2026.236.2058
Successfully tagged ghcr.io/opencharly/arch:2026.236.2058
```

EXIT 0 — the layer-nvidia@2120 tag composes cleanly. Retained: `/tmp/c23-nvidia-build.log`.

**Whole-project validation at THIS head** (covers the refs inside the `cachyos-gpu` (vm deploy) and `charly-cachyos-app` (local deploy) definitions — they reference the identical layer-nvidia@2120 + charly@2040 + layer-cuda@2121 tags proven by the box/nvidia compose and the check-cachyos-vm bed):

```
$ charly box validate
(no errors — EXIT 0)
```

## Fresh composes of ALL changed boxes at THIS head

Every box whose refs this PR changes composes GREEN at this head (logs retained):

```
$ charly box build nvidia           -> COMMIT ghcr.io/opencharly/arch:2026.236.2058  (layer-nvidia@2120 + layer-cuda@2121)  EXIT 0
$ charly box build openclaw-desktop -> COMMIT ghcr.io/opencharly/arch:2026.236.2110  (charly@2040, selkies stack)            EXIT 0
$ charly box build versa            -> COMMIT ghcr.io/opencharly/arch:2026.236.2102  (charly@2040, layer-cuda@2121, gpu)     EXIT 0
$ charly box build githubrunner      -> COMMIT ghcr.io/opencharly/arch:2026.236.2122  (charly@2040, pod-dbus, container-nesting) EXIT 0
```

Retained logs: `/tmp/c23-nvidia-build.log`, `/tmp/c23-openclaw-build.log`, `/tmp/c23-versa-build.log`, `/tmp/c23-githubrunner-build.log`. The three boxes the earlier validator flagged (openclaw-desktop, versa, nvidia) all build EXIT 0 at this head — the pixelflux/maputnik failures seen in an EARLIER head run are resolved by the tag advances in this PR (the charly@2040 tag carries the unified pac install body + key-FILE mechanism + the mkinitcpio/pacman-hook allowances; the standalone layer tags carry their own fixes).

**Whole-project validation at THIS head** (covers the refs inside the `cachyos-gpu` (vm deploy) and `charly-cachyos-app` (local deploy) definitions — they reference the identical layer-nvidia@2120 + charly@2040 + layer-cuda@2121 tags proven by the box/nvidia compose and the check-cachyos-vm bed).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched (ref-only yml edits) |
| R1 | The openclaw-desktop (pixelflux) and versa (maputnik) compose failures seen in an earlier head run were RCA'd to the candies' OWN build steps; at THIS head both boxes compose EXIT 0 (logs retained) — the failures do not survive the tag advances. The check-cachyos-vm deploy-add failure (`target not found: charly`) was RCA'd to the pac host cell lacking repo/key handling — resolved by the charly@2040 unified install body (charly#402) + the arch key-FILE mechanism |
| R2 | No deferral — every surfaced failure is resolved at this head (all four changed boxes compose EXIT 0; the check-cachyos-vm bed passes steps=9) |
| R3 | N/A — one remote-ref mechanism (the resolver's @github fetch) owns the resolution |
| R4 | N/A — the refs pin merged immutable tags |
| R4a | N/A — no mechanism-level workaround introduced |
| R5 | The sweep is pasted in How tested: zero leftover `charly/candy/<moved>` refs at this head (excluding CHANGELOG); the old charly-library refs are deleted in this PR |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R8 | N/A — no generation changes |
| R9 | The diff changes refs inside the `cachyos-gpu` (vm deploy) and `charly-cachyos-app` (local deploy) definitions — they reference the identical layer-nvidia@2120 + charly@2040 + layer-cuda@2121 tags proven by the box/nvidia compose and the check-cachyos-vm bed at this head; `charly box validate` on the whole project passes at this head |
| R10 | The check-cachyos-vm bed (disposable: true) ran at this head — PASS steps=9, deploy-add 0 errors (retained `.check/check-cachyos-vm/2026.236.2046/`); all four changed boxes (nvidia, openclaw-desktop, versa, githubrunner) compose EXIT 0 at this head (logs retained); vm-build PASS `[allowlisted=6]` (the mkinitcpio/pacman-hook allowances ride the charly@2040 tag) |

## Change classification

- **Change class**: candy ref advance (charly-library refs → standalone repos + charly candy bump to v2026.236.2040)
- **Verification gate**: fresh-rebuild check-cachyos-vm bed run at this head (PASS steps=9, 0 errors) + box/nvidia compose + whole-project validate + the org validator
- **Attribution tier**: `analysed on a live system` — the changed refs composed live into this repo's bed and nvidia box (retained output); the org validator is the gate still to pass

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*