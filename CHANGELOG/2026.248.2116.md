# 2026.248.2116 — instrument: check-instrument-cachyos-vm bed (Cutover A)

## Summary

New disposable VM bed per the nested-capture plan (Cutover A): `instrument:` block with a spice session (fps 5, MJPEG capture + transcode pipeline to mp4) + a `record:` session (terminal, asciinema .cast); plan = subject steps only. Config migrated to the schema head 2026.248.1030.

Diff items accounted for:
- **check-instrument-cachyos-vm (new bed)**: `instrument:` block (screen spice session + term record session), subject-prove plan step probing the actual charly delivery path (`ls /tmp/charly-*` — the deploy delivers to a versioned /tmp path, never /usr/bin/charly).
- **cachyos-vm**: bakes `tmux` + `asciinema` into the base image so the golden carries them for clone beds (a clone bed uses the golden's disk — no image rebuild, so the recording tools must be baked).
- **cachyos-vm-clone**: `cloud_init_clean: true` (the golden's cloud-init state must not leak into clones), `spicevmc` channel + `graphics: spice` + `video: virtio` (the SPICE display the screen instrument captures).
- **check-vm-clone-base**: `keep_venue: true` → `false` (the base-golden bed must not keep its venue between runs).

The transcode pipeline belongs on the `screen` instrument (the media contract's screen.mp4 comes from the spice session's MJPEG — the transcode verb's source), not the `term` instrument (whose .cast is the artifact; the transcode verb is single-purpose MJPEG→MP4 and rejected the .cast source).

## How tested (pasted)

**Fresh-rebuild confirmation (R10):** the run below is the FULL R10 sequence on the final tree — build → check → deploy → check-live → **FRESH `charly update` (destroy+recreate)** → check-live-rebuild → cleanup — with pasted output for every step, at zero warnings (all allowlisted dev-worktree notices).

```
$ cd distro-cachyos && CHARLY_PLUGIN_DIR=/tmp/proof-plugins charly check run check-instrument-cachyos-vm
charly check run check-instrument-cachyos-vm: [vm-build] PASS after 1.042s
charly check run check-instrument-cachyos-vm: [vm-create] PASS after 649ms
charly check run check-instrument-cachyos-vm: [deploy-add] PASS after 26.221s
charly check run check-instrument-cachyos-vm: [bring-up-members] PASS after 0s
charly check run check-instrument-cachyos-vm: [check-live] PASS after 5.098s
charly check run check-instrument-cachyos-vm: [update] PASS after 24.558s
charly check run check-instrument-cachyos-vm: [check-live-rebuild] PASS after 3.648s
charly check run check-instrument-cachyos-vm: [cleanup] PASS after 2.358s
charly check run check-instrument-cachyos-vm: [cleanup-members] PASS after 0s
charly check run check-instrument-cachyos-vm: PASS (steps=9) — repo override active
EXIT: 0
```

Evidence envelope (run 2026.248.2108): spice session captured 39 MJPEG frames (759KB, 1280x800); transcode pipeline produced frames.mp4 (h264, 1280x800, verified with ffprobe); record session produced a valid asciinema v3 .cast; evidence.yml carries all rows with venue-scoped ids.

```
$ charly box validate
OK - checked 505 candies, 40 boxes, 36 deploys; 0 warnings, 0 errors
```

**Disposable statement (B17):** the target `check-instrument-cachyos-vm` is explicitly marked `disposable: true` in its `vm:` body.

## Rulebook compliance

- R1: the spice stop hop (row.json finalize) RCA'd to the recorder's ctx-kill at spawn (plugin-check#23) + the log_dir threading gap (plugin-spice#7) + the transcode source threading (plugin-media#1) — all fixed and proven on this bed. No surviving issue.
- R2: fixes landed at the source (the three plugin PRs), not workarounds.
- R3/R5/R6: one bed config; migrated not dual-kept; PR-only.
- R10/RDD: the full R10 sequence (build→check→deploy→check-live→update→check-live-rebuild→cleanup) PASSES on the final tree — no deferral.
- ADE/SDD: N/A for a bed config; singular naming (instrument/phase).

## Change classification

Class: bed config. Gate: the full R10 bed run (steps=9, exit 0) + box validate. Attribution: fully tested and validated (bed run + box validate on the final tree).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*