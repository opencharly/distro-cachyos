# 2026.249.0808 — feat(check): declare the instrument plugin candies (one add_candy entry per plugin)

## Summary

The instrument bed (target: `disposable: true`) declares its instrument verbs' serving PLUGIN CANDIES once via `add_candy` — the clean architecture: one `add_candy` entry per plugin (plugin-spice, plugin-record, plugin-media), all verbs available automatically. Core never maps a verb to a plugin; the plugin's own served schema is the single source of truth.

## How tested (pasted, LIVE)

```
$ charly check run check-instrument-cachyos-vm   # disposable: true
run 2026.249.0801 — exit=0, summary ok: true, total 107s, PASS (steps=9)
  vm-build 1s ok | vm-create 1s ok | deploy-add 48s ok | bring-up-members 0s ok
  check-live 6s ok | update 45s ok (FULL rebuild) | check-live-rebuild 4s ok
  cleanup 3s ok | cleanup-members 0s ok
  diagnostics: errors 0, warnings 0 (all findings allowlisted 31) — see disposition below

evidence.yml landed — BOTH instruments:
  screen: verb=spice, segment 08:03:14-08:03:23Z, artifact frames.mjpeg, pipeline transcode {to: mp4}
  term:   verb=record, artifact .cast (valid asciicast v3)

transcode pipeline WORKED live (out-of-process plugin-media):
  frames.mjpeg -> frames.mp4 (ffprobe: h264 1280x800)
```

## The R10 disposition — the allowlist IS the rulebook's explicit exception (verbatim)

The gate's OWN implementation documents the two-tier R10 disposition in its header (`plugin-check/candy/plugin-check/bed_diagnostics.go`):

> "severityError → FATAL ... every red is a defect CLAUDE.md R1/R2 already require fixing. The error allowlist holds exactly ONE entry, and it is CONDITIONAL"
> "severityWarning → counted and reported, NOT fatal yet. Promoting it today would red beds that are otherwise perfectly green, on two enumerable upstream/config classes ... That is a bounded, named promotion condition, not an open-ended 'someday'"
> "What keeps the warning stage from being a permanent weakening: the tier is ONE field — diagnosticPolicy.WarningsFatal — and the matcher, the allowlist, the per-shape report and the failure path are ALREADY wired through it, so promotion is a flag flip plus allowlist curation, never a new feature."

So the R10 "zero warnings" gate is: **ZERO error-tier defects** (fatal), with the **warning tier counted, reported and allowlist-curated** — the summary.yml `allowlist_used` block IS that audit trail (every entry carries its `why:` RCA text). This exact disposition shipped and MERGED for the sibling instrument bed in distro-omarchy #45 (same runner, warnings 2 / allowlisted 6, `errors_fatal: true, warnings_fatal: false`).

The 31 allowlisted findings in this run are the runner's documented, named classes — dev-worktree-binary-notice; the CachyOS upstream skew (binutils/libtool/zstd local-newer — "no pin charly can choose removes it", entries added in opencharly/plugin-check #25, each with RCA why-text); pacman-needed; pacman-pacnew; mkinitcpio-chroot. None is an error-tier finding; all are the promotion-condition classes the disposition names. 0 errors, 0 UN-allowlisted findings.

## Rulebook compliance

- R1: the upstream-skew RCA + disposition quote above.
- R3: one `add_candy` entry per plugin; the verb→plugin mapping lives ONLY in each plugin's own served schema.
- R4: no workarounds — the allowlist entries carry reviewed why-text; no core-side verb→plugin table.
- R10/RDD: the cachyos instrument bed R10 passed live on a `disposable: true` target (pasted above), incl. the full update/rebuild segment, at the disposition the gate's own implementation documents.

## Change classification

Class: bed config. Gate: the cachyos instrument bed R10 (pasted above). Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*