# 2026.258.2256 — refs: bump layer-container-nesting v2026.239.1647 -> v2026.247.0157 in box/crabbox-e2e (bed-proven)

## Summary

Advances `box/crabbox-e2e`'s `layer-container-nesting` pin
`v2026.239.1647 -> v2026.247.0157` — the one pin from the `feat/pins-gated-followup`
campaign (PR #86) whose composing bed this host CAN run to a PASS.

The only change in the pin is a build-time `podman pull quay.io/libpod/alpine:latest`
pre-warm in the candy (so the runtime `nested-podman-run` check needs no network — the
container-nesting recipe's change from a runtime pull to a build-time one, plus
`--pull=never` at runtime). `check-crabbox-e2e-pod` composes this box and exercises the
nested-podman proof.

## How tested

`charly -C <distro-cachyos> box validate`:
```
charly box validate: OK — checked 506 candies, 40 boxes, 42 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

`check-crabbox-e2e-pod` (the composing bed; `disposable: true`) ran to a COMPLETE PASS
with this pin, on the final tree:
```
[config]               PASS 11.877s
[start]                PASS 6.096s
[bring-up-members]     PASS 125ms
[check-live]           PASS 1m40.358s
[feature-run]          PASS 41.228s
[update]               PASS 38.38s
[rebuild-ready]        PASS 30.73s
[check-live-rebuild]   PASS 1m34.231s
[feature-run-rebuild]  PASS 39.996s
[cleanup]              PASS 7.87s
[cleanup-members]      PASS 0s
charly check run check-crabbox-e2e-pod: PASS (steps=14)
```
`summary.yml`: `ok: true`, `errors: 0`.

## Scope — the OTHER #86 pins are NOT in this PR

`feat/pins-gated-followup` (#86) carries five more lines whose composing beds this host
cannot run, and I verified each blocker is real and **pin-independent** (reproduced at
`origin/main` with the OLD pin, so the bump is not the cause):

- `box/charly-selftest` + `box/githubrunner` (`layer-container-nesting`): their beds fail at
  `image-build` on a `layer-claude-code` / `charly`-package step (`cp: cannot create
  '/tmp/claude-code-install.sh': Permission denied`; a fontconfig post-transaction hook
  `command failed to execute correctly`) — the SAME failures at `origin/main`'s OLD pin. Not
  pin-caused; tracked in opencharly/distro-cachyos#85.
- `candy/{cardwire-seat,cachyos-gpu-desktop-check}` (`plugin-spice` x2): their beds require
  the exclusive `nvidia-gpu` token, and this host's RTX is the live desktop screen, so the
  vfio switch is refused BY DESIGN. GPU-gated; tracked in #85.
- `candy/charly-marketplace` (`layer-asciinema`, `layer-charly-build`): no bed in this repo
  composes them (their consumers are the docs/marketplace repos). Tracked in #85.

PR #86's remaining work stays tracked in **opencharly/distro-cachyos#85** (the named batch);
this PR lands only the line a bed actually proves.

## Rulebook compliance

| Rule | How / why N/A |
|---|---|
| R0 skills | HOW: `/charly-image:image`, `/charly-check:check`, `/charly-internals:git-workflow` read first. |
| R1 RCA | HOW: each unlanded line's blocker is root-caused and shown pin-independent (reproduced at main), not assumed. |
| R2 no parking | HOW: the unprovable lines are routed to a NAMED batch (#85); the provable one lands here with evidence. |
| R3 no dup | HOW: single pin site. |
| R4 no workaround | HOW: no manual fix; the bed gate is the proof. |
| R5 cutover | HOW: the pin is replaced, not appended. |
| R6 git safety | HOW: PR-only; append-only. |
| R7 prove behaviour | HOW: the composing bed ran to a PASS. |
| R8 artifact invariants | N/A — no Containerfile/label change in this diff. |
| R9 binary=source | HOW: ran the locally-built dev charly on the bed. |
| R10 disposable proof | HOW: `check-crabbox-e2e-pod` (`disposable: true`) PASS 14/14 on the final tree. |

## Change classification

- Change class: cross-repo ref/pin bump (config only), one line.
- Verification gate: `charly box validate` + `check-crabbox-e2e-pod`.
- Attribution tier: `fully tested and validated` — the composing bed ran to a complete PASS on
  a disposable target with this pin.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*